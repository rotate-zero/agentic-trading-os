# Decision Index


One line per confirmed decision, #1 through #98, across every file in this directory. Read this first — it's a fraction of the cost of reading the full history, and points to exactly which file has the full text of anything relevant to the current task. See `README.md` for the full read/write/maintain protocol.

| # | Summary | File |
|---|---|---|
| 1 | Broker order of implementation — REVISED. | `archive/001-060.md` |
| 2 | Candle storage | `archive/001-060.md` |
| 3 | Scanner cadence | `archive/001-060.md` |
| 4 | Redis | `archive/001-060.md` |
| 5 | Context Engine is a composed aggregator | `archive/001-060.md` |
| 6 | Governor's decision schema is widened now, implemented narrow. | `archive/001-060.md` |
| 7 | World View is a read-only composite facade | `archive/001-060.md` |
| 8 | Event data contracts are formalized before Phase 1 code | `archive/001-060.md` |
| 9 | Event Bus dispatch is two-lane, not multi-tier priority. | `archive/001-060.md` |
| 10 | Stateful engines that need periodic-but-not-constant recompute (Market State Engine, Position… | `archive/001-060.md` |
| 11 | `docs/` is the authoritative location for all architecture and design documentation | `archive/001-060.md` |
| 12 | Partition-boundary literals in migrations must carry an explicit UTC offset (`+00`), never a bare… | `archive/001-060.md` |
| 13 | Use `ib_async`, not `ib_insync`, for the IBKR adapter. | `archive/001-060.md` |
| 14 | IBKR 2FA: switch to IB Key, not SMS; Gateway set to auto-restart, not auto-logoff. | `archive/001-060.md` |
| 15 | `BrokerAdapter`'s `Tick` type includes `symbol`, distinct from the Event Bus's `PriceUpdated`… | `archive/001-060.md` |
| 16 | Tick-to-candle bucketing lives above the adapter, not inside it. | `archive/001-060.md` |
| 17 | `is_connected()` is part of the `BrokerAdapter` ABC itself, not an IBKR-specific extension. | `archive/001-060.md` |
| 18 | One shared adapter registry (`app/services/broker_registry.py`), not per-route globals. | `archive/001-060.md` |
| 19 | Participation added as a 7th Market State dimension. | `archive/001-060.md` |
| 20 | Context Engine v1 provider list expanded: `SectorCorrelationProvider` and `NewsFlagProvider`. | `archive/001-060.md` |
| 21 | `ContextProvider` is explicitly generalized to arbitrary refresh cadence, not just… | `archive/001-060.md` |
| 22 | Manual trading reuses the existing pipeline via a single `plan(TradeRequest) -> TradePlan`… | `archive/001-060.md` |
| 23 | Manual trade input is abstracted behind a device-agnostic Input Layer, not built for a specific… | `archive/001-060.md` |
| 24 | Hotkey targeting resolves against an explicit, per-tab `FocusedTile`, changed only by deliberate… | `archive/001-060.md` |
| 25 | A single `LONG`/`SHORT` action per direction replaces the… | `archive/001-060.md` |
| 26 | Hotkey actions are modeled generically: Action Categories route dispatch, Safety Levels gate… | `archive/001-060.md` |
| 27 | `FocusedTile` renamed `TradeTarget`; explicit non-triggers documented. | `archive/001-060.md` |
| 28 | Split `BrokerAdapter` into `MarketDataProvider` (base) + `BrokerAdapter(MarketDataProvider)` (adds… | `archive/001-060.md` |
| 29 | `ib_async` pinned to `>=2.1,<3.0`, not `<2.0`. | `archive/001-060.md` |
| 30 | `PolygonAdapter` built around free-tier constraints, not against an assumed real-time feed. | `archive/001-060.md` |
| 31 | `IBKRIngestBridge` renamed `TickIngestBridge`, retyped against `MarketDataProvider`. | `archive/001-060.md` |
| 32 | `FinnhubAdapter` built for genuine real-time streaming; historical stock candles explicitly not… | `archive/001-060.md` |
| 33 | `broker_registry` split into two independent named roles — `streaming` and `historical` — replacing… | `archive/001-060.md` |
| 34 | `main.py`'s auto-connect calls the same shared `connect_polygon()`/`connect_finnhub()` functions… | `archive/001-060.md` |
| 35 | Frontend mock-swap resumed and completed for `candles.ts`; a new generic `POST /market/subscribe`… | `archive/001-060.md` |
| 36 | `generateMockOverlays` (`mocks/chartObjects.ts`) needed an empty-candles guard, since… | `archive/001-060.md` |
| 37 | `UnhandledExceptionMiddleware` (`app/core/error_handling.py`) added as a plain ASGI middleware, NOT… | `archive/001-060.md` |
| 38 | Tests now blank `FINNHUB_API_KEY`/`POLYGON_API_KEY` for the duration of every test… | `archive/001-060.md` |
| 39 | `PolygonAdapter.get_historical()` (and its polling counterpart) now categorizes the free/Basic… | `archive/001-060.md` |
| 40 | Feature Engine kickoff: chart-pane SMA rebuilt as an instance-based model (`PriceIndicatorInstance`… | `archive/001-060.md` |
| 41 | Feature Engine chart-reading indicators expanded significantly: EMA fully migrated onto the SMA… | `archive/001-060.md` |
| 42 | Chart zoom-reset bug fixed; candle-close latency bug fixed; volume bars made fully customizable;… | `archive/001-060.md` |
| 43 | The `candles`/`symbols` tables (scaffolded Phase 2, unused since) now have a writer and a reader —… | `archive/001-060.md` |
| 44 | Frontend chart timeframe wiring fixed at the root; backend gained session-local aggregation for… | `archive/001-060.md` |
| 45 | Backend Feature Engine started: server-side SMA, published as `FeaturesUpdated` on every `1m`… | `archive/001-060.md` |
| 46 | Level Interaction Engine built: touch/holding/rejected/conquered tracking for any level… | `archive/001-060.md` |
| 47 | Backend read side for the Feature Engine panel: `get_snapshot()` on both engines, `GET… | `archive/001-060.md` |
| 48 | Feature Engine panel built (frontend) — `GET /intelligence/state`'s first real consumer — plus a… | `archive/001-060.md` |
| 49 | First real browser feedback on the Feature Engine panel (decision #48) — one real gap closed, one… | `archive/001-060.md` |
| 50 | Chart indicators redirected onto the Feature Engine migration path — decisions #40/#41's… | `archive/001-060.md` |
| 51 | Feature Engine extended to 5m/15m/1h — Stage 2 of the chart migration (decision #50), "Option A2":… | `archive/001-060.md` |
| 52 | EMA added to Feature Engine — Stage 3's first indicator (decision #50), resolving D3: full-window… | `archive/001-060.md` |
| 53 | VWAP added to Feature Engine — Stage 3's second indicator (decision #50). | `archive/001-060.md` |
| 54 | Stage 1 of the chart migration (decision #50): the Chart now actually consumes Feature Engine for… | `archive/001-060.md` |
| 55 | A real local-environment failure, diagnosed and fixed, plus `feature_engine/indicators.py` split… | `archive/001-060.md` |
| 56 | PDH/PDL/PDC, Camarilla pivots, and pre-market H/L added to Feature Engine — Stage 3's remaining… | `archive/001-060.md` |
| 57 | VPOC added to Feature Engine — Stage 3's last indicator, resolving D5 more simply than that… | `archive/001-060.md` |
| 58 | Stage 1 extended to cover all seven horizontal-level types from decisions #56–57 — PDH/PDL/PDC, all… | `archive/001-060.md` |
| 59 | Daily Levels — concept locked across a three-way review (Saqib + Claude + Grok + ChatGPT),… | `archive/001-060.md` |
| 60 | Daily Levels Stage 1 built and tested — clustering algorithm, same-candle validity gate,… | `archive/001-060.md` |
| 61 | Daily Levels Stage 4 built and tested — the indicator is now on the chart. | `archive/061-079.md` |
| 62 | Daily Levels: root-caused Saqib's "no levels showing" report, then shipped a price-range filter, a… | `archive/061-079.md` |
| 63 | Daily Levels Stage 2 built and tested — persistent, price-proximity-reconciled level identity, and… | `archive/061-079.md` |
| 64 | Daily Levels Stage 3 built and tested — LevelInteractionEngine now tracks… | `archive/061-079.md` |
| 65 | Daily Levels menu panel scrollbar fixed, and D1 (the last open item across all four build stages)… | `archive/061-079.md` |
| 66 | Two small standing open items picked up together, deliberately staying off the Trading Intelligence… | `archive/061-079.md` |
| 67 | Feature Engine indicator expansion — direction locked for five new families (ATR, Session % / $… | `archive/061-079.md` |
| 68 | Feature Engine indicator expansion — D1/D2/D3 resolved by Saqib directly (shared daily-candle… | `archive/061-079.md` |
| 69 | Feature Engine indicator expansion — Stage 2 (ATR(1D,14) + ATR%) built and tested against a real… | `archive/061-079.md` |
| 70 | Feature Engine indicator expansion — Stage 3 (Linear Regression) and Stage 4 (KAMA) built together… | `archive/061-079.md` |
| 71 | Relative Volume (RVOL) built and tested against a real local Postgres — a sixth Feature Engine… | `archive/061-079.md` |
| 72 | LiveTickRelay — throttled "tick fluidity" for the chart's currently-forming 1m bar, on a small (max… | `archive/061-079.md` |
| 73 | Chart Style — candlestick vs. | `archive/061-079.md` |
| 74 | Volume Avg lines gained a per-line price-axis label toggle (`showPriceLabel`) — closes the one… | `archive/061-079.md` |
| 75 | On-chart HUD text box — a floating, per-line-configurable Feature Engine readout, pinned to the top… | `archive/061-079.md` |
| 76 | Dropdown-panel clipping fixed for all 5 real panels in the app — a shared placement hook… | `archive/061-079.md` |
| 77 | Opacity added to every hex-color field in the app — SMA/EMA/VWAP lines, horizontal levels, Timer,… | `archive/061-079.md` |
| 78 | Backfilled entry — Saved Layouts (named save/load/delete, JSON export/import) already exists in… | `archive/061-079.md` |
| 79 | `confirmed-decisions.md` split into an open file + frozen archive + index, to keep session-start reading cost bounded | `archive/061-079.md` |
| 80 | Volume Bars opacity confirmed already shipped (#77); real bug was `useDropdownPlacement` inflating panel max-height past available viewport space on narrow/short screens — fixed in the shared hook for every dropdown panel at once. | `confirmed-decisions.md` |
| 81 | Axis-label name half ("PDH", "SMA 9") gained its own on/off toggle (`showNameLabel`), separate from the existing price-value toggle (`showPriceLabel`) — closes a Lightweight Charts quirk where an indicator's name stayed visible even with the value hidden. | `confirmed-decisions.md` |
| 82 | #81's `showNameLabel` toggle extended to Volume Avg lines and Daily Levels, the two indicator types it deliberately left out — every label-bearing indicator now follows the same on/off convention. | `confirmed-decisions.md` |
| 83 | SMA/EMA slope-as-angle, computed in the Feature Engine (live + historical paths), percentage-normalized (not ATR — same cross-timeframe objection as #67), lookback tied to each period, surfaced as a numeric `∠+35.2°` label suffix behind a new per-instance toggle. | `confirmed-decisions.md` |
| 84 | `LevelInteractionEngine.stop()`'s FK-violation-on-teardown race root-caused to `task.cancel()` not actually stopping an in-flight `to_thread` DB write; fixed with a poison-pill queue drain. `CandleRecorder`/`FeatureEngine` flagged as likely sharing the same latent bug, not yet fixed. | `confirmed-decisions.md` |
| 85 | Two SMA/EMA slope gaps #83 itself flagged and deferred, fixed together: `sma_9_slope`/`_r2`/`_slope_pct`/`_slope_angle` (and `ema_`) now (a) nest under the owning period's Feature Engine panel entry instead of rendering as their own bogus standalone units, and (b) are excluded from Level Interaction Engine tracking instead of getting real zone/touch classifications against `close`. Regression/KAMA's identical gaps flagged, not fixed — deliberately scoped to SMA/EMA only. | `confirmed-decisions.md` |
| 86 | Closes the broader Level Interaction Engine tracking gap #83/#85 flagged and deferred: ATR/ATR% (excluded wholesale), Gap, Session Change, RVOL, Premarket Volume Ratio, session volume totals, and Regression/KAMA's own derived slope/deviation/r2/dist/efficiency byproducts are now excluded too. `regression_{period}_value` decided to stay tracked — same "reference line kept, its own delta excluded" split already established for `kama_{period}`. | `confirmed-decisions.md` |
| 87 | Strategy Engine direction locked (Stage 0, no code): four-stage `evaluate()` anatomy, per-strategy + declarative Gate, versioned/immutable `StrategyConfig`, `Opportunity`'s `evidence`/`structural_invalidation`/`structural_target`, Performance Intelligence's `StrategyOutcome` schema (query-time ranking, never a stored rank), Decision Engine + Governor's distinct roles consuming performance evidence, and a Backtest Runner scoped as an extension of the deferred Replay Engine. Full plan: `docs/architecture/strategy-engine-design.md`. Bar-close entry-timing left deliberately open for further discussion. | `confirmed-decisions.md` |
| 88 | Strategy Engine §8 (entry timing) resolved — supersedes #87's "left open" framing for this item. ACT/WAIT/ABANDON model locked, not "wait for bar close": `Opportunity` gains `status`/`wait_reason`/timestamps, `StrategyConfig` gains a bare `allows_waiting` flag (not a `confirmation_timeframe` enum). Two structure gaps identified and kept distinct — `FeatureSet` missing OHLC (schema gap) vs. `PriceSnapshot`'s already-existing live OHLCV having no consumer (wiring gap). Waiting-value model itself and live-structure wiring both explicitly deferred. Still Stage 0, no code. | `confirmed-decisions.md` |
| 89 | Performance Intelligence's `StrategyOutcome` schema refined and locked — supersedes #87's version of §5. Table renamed `strategy_performance` → `strategy_outcomes`. Fields reorganized into six groups (Identity & Versioning, Timing, Ledger, Thesis, Evidence, Backtest Provenance) under three pillars (Ledger/Evidence/Provenance). New `schema_version` field, kept distinct from `strategy_version`. `market_state_at_signal`/`context_at_signal` renamed `_at_entry`, now paired with new `_at_exit` counterparts (open item D7 tracks reintroducing a true `_at_signal` pair later). `realized_pnl` defined explicitly as net of commission; `entry_qty`/`exit_qty` equality enforced by write-time assertion; new `slippage_entry`, `trading_day`, `holding_seconds`, `feature_snapshot_id` fields; `exit_reason` gains `eod_flatten` for the day-trading-only rule. `backtests` table shape locked, including `data_version`/`feature_version` for reproducibility. `exit_trigger` raised and deliberately deferred — `future-ideas.md` #21. Still Stage 0, no code. | `confirmed-decisions.md` |
| 90 | Context Engine's boundary redefined and locked — supersedes the never-pushed original #90 draft's open-question framing. Governing rule: price/volume comparisons belong to Market State, not Context, regardless of symbol count. `GapProvider`, `LevelsProvider`, `VolatilityRegimeProvider` cut entirely (thin-wrapper option considered and rejected). `SectorCorrelationProvider` split: static membership → `symbol_fundamentals` field, no provider needed; dynamic price-relationship → Market State's cross-symbol layer (decision #91), deferred (`future-ideas.md` #24). `FundamentalsProvider` confirmed (Finnhub-sourced, `symbol_fundamentals` table, `market_cap` refresh split from slower profile fields). `NewsFlagProvider` no-storage rule confirmed, output widened to `present`/`count_15m`/`recency_seconds`/`importance`. Still Stage 0, no code. | `confirmed-decisions.md` |
| 91 | Market State Engine's v1 shape locked. Every dimension is a `<dimension>_score` (0–100), not a categorical tag — scores are state measurements, never probabilities. v1 ships scores only: no bands, no duration-in-band, no confidence, no `previous`/`changed_at` (full classification system deferred, `future-ideas.md` #22); scores are the only thing ever persisted, tags always computed live, never stored. Per-symbol v1 dimensions: Trend, Volatility regime, Volume regime, VWAP relationship, Session type, Acceleration (promoted from a nested "Strength" field). `Market breadth` removed from the per-symbol list — full breadth deferred (`future-ideas.md` #23); Participation stays blocked, unaffected. SPY/QQQ/IWM pulled forward from Phase 5 scaffolding to v1 as always-on cross-symbol subjects; new small `CrossSymbolState` composite (`spy_direction_score`, `qqq_direction_score`, `iwm_direction_score`, `trend_alignment_score`, `risk_on_score`, `qqq_leadership_score`, `iwm_confirmation_score`), persisted as a sentinel row in the existing `market_state_history`, no new table. Four-part governing rule added to `trading-intelligence-architecture.md` §2: "Feature Engine measures. Market State interprets. Context Engine describes the world outside the market. Strategy decides." One item left explicitly open: whether the original restart-rebuild-from-history decision still applies now that duration-in-band is deferred. Still Stage 0, no code. | `archive/091-106.md` |
| 92 | Context Engine's M1 slice built — `ContextProvider` ABC, `ContextEngine` aggregator, `ContextChanged` event schema, and `CalendarProvider` (the only provider buildable before M0's Finnhub spike results land). Two deliberate deviations from §5's original interface: `evaluate()` drops the not-yet-built `MarketState` parameter; the aggregator's v1 trigger is a session-boundary loop (`MarketClock.next_session_boundary()`), not a `MarketStateChanged` subscription. `CalendarProvider` adds a small hardcoded 2026 FOMC-date set for Fed-day awareness, which `MarketClock` itself doesn't have. `system-design.md` §10.3's stale `ContextChanged` example (still listing `gap`/`levels`/`volatility_regime`, cut by decision #90) corrected in the same change. | `archive/091-106.md` |
| 93 | Market State Engine's M2 slice built — per-symbol only. `MarketState`/`MarketStateChanged` schema, `MarketStateEngine` (per-symbol `DebounceScheduler` for fast-only triggering, single poison-pill worker — decision #84, reused, not reinvented — for all compute/persist/publish), `market_state_history` migration, and `app/market_state_engine/scoring.py`'s four pure scoring functions (Trend, Volatility regime, Volume regime, VWAP relationship) plus Acceleration (Trend's own rate of change). Two deviations from decision #91's six-dimension list: `Session type` dropped entirely (doesn't fit the directional/magnitude shape, flagged to revisit); `Acceleration` scoped to Trend only, not one value per dimension. `system-design.md` §10.3's pre-score-first `MarketStateChanged` row, and `models/trading_intelligence.py`'s stale claim that Market State rebuilds from persisted history on restart (superseded by #91), both corrected in the same change. Cross-symbol (`CrossSymbolState`, SPY/QQQ/IWM) is M3, not built here. | `archive/091-106.md` |
| 94 | M0's Finnhub Context Engine spike results locked — `FundamentalsProvider`'s TTM derivation and Finnhub's real two-bucket rate-limit structure, both found empirically rather than assumed from documentation. `financials-reported`'s quarterly figures turned out to be fiscal-year-to-date cumulative, not discrete — found via a self-consistency check (quarters summing to 124%/62% more than the annual figure), a first duplicate-concept hypothesis tested and refuted, then confirmed via differencing consecutive same-fiscal-year quarters and validated against real AAPL/TSLA revenue and net income (reconstructed TTM landing within single-digit % of sensible YoY growth). Finnhub's 60/min limit found to be two separate buckets by endpoint category (profile2+financials-reported share one; calendar/earnings+company-news share another), confirmed with exact predictive math across four independent runs. `company-news` confirmed usable for `NewsFlagProvider` (real per-article unix timestamps) but restricted to single-name symbols — ETF tickers return generic broad-market news mislabeled as related. Still Stage 0, no code. | `archive/091-106.md` |
| 95 | M0's SPY/QQQ/IWM + Participation spike results locked — Polygon daily bars confirmed clean (10 bars/14-day window, no gaps, healthy volume/price ranges, no ETF-specific quirks), and Finnhub's free-tier trade feed's IEX-only scope documented as a load-bearing constraint. Finnhub's WS trades confirmed IEX-only via its own docs, not assumed; IEX's own Q2 2026 stats put it at ~10% of ETF volume even at its best, explaining a live capture where QQQ got 47 trades in 30s while SPY and IWM got zero in the same window (subscription-ordering bug checked and ruled out directly — QQQ was the middle-subscribed symbol, not first or last). Tick-rule signed volume mechanically proven derivable (demonstrated live against QQQ's captured trades, real condition codes) but must be scoped/named as IEX-specific if built, not general market participation — recommends a longer aggregation window over raw tick-by-tick given the demonstrated sparsity. Participation's blocked status (decision #91) unchanged by this entry. Still Stage 0, no code. | `archive/091-106.md` |
| 96 | M1's remainder built — `symbol_fundamentals` migration, `FundamentalsProvider`, `NewsFlagProvider`, `FundamentalsRefreshJobs` (3 independent weekly/daily loops), and decision #94's validated TTM derivation reimplemented as pure, tested functions (`app/context_engine/fundamentals_derivation.py`). Surfaced and resolved a real architectural gap: `ContextProvider` was implicitly market-wide, but Fundamentals/News are per-symbol — added `SymbolContextProvider` and `ContextEngine.evaluate_for_symbol()` as a second, parallel aggregation path (15-minute per-symbol timer, not tick-driven), `evaluate_all()` untouched. `sector` left permanently NULL — Finnhub provides one classification field, not a sector-and-industry pair decision #90's schema assumed. NewsFlagProvider never calls `/company-news` for SPY/QQQ/IWM (decision #94's ETF-news finding, load-bearing immediately since SPY is already in the seeded Scanner Universe). Persistence reuses decision #84's cancellation-safety fix (fast non-blocking triggers, a single poison-pill worker) rather than risk reintroducing the bug it already fixed elsewhere. Found and fixed a real off-by-one bug in the TTM gap-fill logic via synthetic tests before it ever touched real data, and a real test-suite entanglement gap (ContextEngine's new per-symbol bootstrap silently made previously DB-free tests DB-dependent). | `archive/091-106.md` |
| 97 | Market State Engine's M3 slice built — `CrossSymbolState` (SPY/QQQ/IWM cross-symbol synthesis: `trend_alignment_score`, `risk_on_score`, `qqq_leadership_score`, `iwm_confirmation_score`, plus a passthrough of each symbol's own `trend_score`), closing out decision #91's per-symbol/cross-symbol split. Rides the same per-symbol `DebounceScheduler` pipeline #93 built (tighter ~4s ceiling for SPY/QQQ/IWM), persists as a `__MARKET__` sentinel row in the existing `market_state_history` table (4 original score columns relaxed to nullable, 7 new nullable columns added), publishes via the existing `MarketStateChanged` event type distinguished by `envelope.symbol`. Two-row-shape split enforced by a write-time assertion, decision #89's `entry_qty==exit_qty` precedent reused. Migration drafted as `0006` in an earlier session, renumbered to `0007` after decision #96's `symbol_fundamentals` migration claimed `0006` first in parallel work. Surfaces, but deliberately doesn't resolve, whether `ContextProvider.evaluate()` should now take the `MarketState`/`CrossSymbolState` parameter decision #92 deferred — flagged for a direct decision with Saqib, not bundled into this build. | `archive/091-106.md` |
| 98 | M4 opened as an integration/contract milestone, scope deliberately narrowed to keep Strategy Engine itself out of it. `get_snapshot(symbol=None)` added to both `MarketStateEngine` and `ContextEngine` — same read-side-cache pattern as `FeatureEngine`/`LevelInteractionEngine` (decision #47); Context's version transparently merges decision #96's two aggregation paths per symbol. New `app/trading_intelligence/state_snapshot.py` — the `StrategyOutcome` snapshot-capture contract (decision #89's `market_state_at_entry/_at_exit`/`context_at_entry/_at_exit`), deliberately just the read contract, no table/writer/execution lifecycle. Two new observability routes, `GET /intelligence/market-state` and `GET /intelligence/context`, kept separate rather than merged into a Strategy-shaped composite. Real gap found and left open: `ContextChanged` has no domain-safe timestamp the way `MarketState.candle_ts` does — Context's cadence is timer-driven, not candle-driven; `get_snapshot()`'s `evaluated_at` is honestly wall-clock, not fabricated as domain-safe. `system-design.md` §4.8's stale "not yet built" caption for Market State/Context corrected. No Replay Engine/Backtest Runner built (task 24 answered via two timestamp findings + a test proving the new cache doesn't substitute wall-clock time, not via building the deferred harness). | `archive/091-106.md` |
| 99 | Strategy Engine Stage 1 built — `base_strategy.py` (real `Strategy`/`StrategyConfig`/`Opportunity`/`ScheduleTrigger`, four interface questions decided: `__init__(config)`, `every_candle(timeframe=...)`, `context: ContextChanged`, and an explicit `symbol: str` parameter on `evaluate()` new to this build), `orb_strategy.py` (ORB, the first concrete strategy, corrected against §1's illustrative time-boxed trigger to `every_candle("1m")`, private per-symbol/per-day opening-range state, fires once per direction per day), and `FeatureSet` gaining `open`/`high`/`low`/`volume` (1m only — aggregated 5m/15m/1h left `None`, true bucket OHLC not tracked) so ORB's range uses real wicks, not an approximation. An undocumented orphan (`momentum_strategy.py`/`vwap_strategy.py`, built by an earlier concurrent session against a `base_strategy.py` that never existed, decision-log entry lost to a numbering collision with #98) was found, discarded rather than reconciled, and its sound MATCH/SCORE logic handed to a separate session to rebuild fresh. Reconciled against a second, independent set of concurrent-session findings from that same discarded work (a real threshold-mirroring bug applied directly to `orb_strategy.py`; a PROPOSE-stage sanity-guard bug confirmed NOT to apply to ORB, and why; a real, still-open `MarketStateEngine._latest_features` per-symbol timeframe race found independently by both reviews, flagged for Saqib rather than fixed here). Also found and flagged, not fixed: `docs/architecture/premarket-accumulator-design.md` incorrectly still reads "DRAFT, no code" despite `vwap_ext` already being fully built in Feature Engine — a documentation-staleness gap outside this build's scope. | `archive/091-106.md` |
| 100 | `vwap_ext`/`session_volume_ext` built — one continuous 4:00am-anchored accumulator (`_update_vwap_ext`, sibling of `_update_vwap` decision #53), closing the platform-VWAP-discrepancy gap. Deliberately additive, `vwap`/`session_volume` untouched. Backfilled — built earlier, never logged until decision #99 found the gap. | `archive/091-106.md` |
| 101 | `premarket_volume_ratio` built, gated on an empirical Polygon 1-minute pre-market data check that was actually run (`scripts/check_premarket_data_availability.py`) before any code was written. Two real bugs found and fixed along the way (a wrong class name in the check script; 6 tests' fake providers not timeframe-aware) plus a pre-existing `datetime.now()` test-flakiness fix, unrelated to this feature. Backfilled, same as #100. | `archive/091-106.md` |
| 102 | Scanner integration for `premarket_volume_ratio` built — wired in via `elif` sharing `rvol`'s activity slot (structural mutual exclusivity, not assumed), new `scanner_weight_premarket_volume_ratio` setting defaulting to 0.0 (wiring decoupled from trusting the ranking). A second pre-existing, unrelated flaky test found and left alone (async shutdown-ordering race, confirmed to predate this work). Backfilled, same as #100/#101 — `premarket-accumulator-design.md`'s stale "DRAFT, no code" status line and `system-design.md`'s `FeaturesUpdated` table (missing `vwap_ext`/`session_volume_ext`/`premarket_volume_ratio` entirely) both corrected in this same change. | `archive/091-106.md` |
| 103 | `MarketStateEngine`'s per-symbol timeframe race fixed — the item decision #99 flagged but deliberately didn't fix. `_latest_features` re-keyed from `dict[str, dict]` (symbol alone — any timeframe's arrival silently overwrote any other's) to `dict[tuple[str,str], dict]` keyed `(symbol, timeframe)`; `_compute()`/`_compute_cross_symbol()` now always read the `(symbol, "1m")` slot specifically, never "whichever arrived last." `_on_features_updated` now only schedules a recompute for 1m arrivals — a direct, narrowly-scoped consequence (5m/15m/1h re-triggering would only re-derive the same MarketState from unchanged 1m data underneath it), not separate scope creep. 4 new DB-free tests directly reproduce the race and prove it's closed. | `archive/091-106.md` |
| 104 | Gap Strategy built — `gap_strategy.py`, fourth strategy against the real `base_strategy.py` (decision #99), delivered with decision #105. Continuation-of-the-opening-gap setup: `gap_pct`/`gap_dollars` read straight off `FeaturesUpdated` per the boundary decision #90 already set, `regular_open` reconstructed algebraically as `pdc + gap_dollars` rather than tracked as new state, gated to `MarketClock.is_regular_session()`, fires at most once per symbol per day (`_GapState.fired: bool` — gap direction can't reverse intraday, simpler than ORB's `fired_directions` set). Built after decision #103 closed the `MarketStateEngine` shared-slot race, so unlike ORB/Momentum/VWAP this strategy has no exposure to flag. 15 new tests, all DB-free. | `archive/091-106.md` |
| 105 | Volume Spike Strategy built — `volume_spike_strategy.py`, fifth strategy against the real `base_strategy.py` (decision #99), delivered with decision #104. Corrects `base_strategy.py`'s own illustrative `on_event("VolumeSpike")` naming (no such event exists anywhere in the codebase) to `every_candle(timeframe="1m")`, with its own new private per-symbol rolling volume baseline (`deque(maxlen=lookback_bars)`, same "own private state" precedent as ORB's opening range — no engine anywhere computes a per-candle volume baseline). Fires on a candle whose volume clears a ratio threshold against that rolling baseline, confirmed by the candle's own bar direction and Market State's trend_score/volume_regime_score; a single per-symbol cooldown timer (not per-day, not per-direction) prevents re-firing on the immediate next candle of the same move while still allowing multiple genuinely independent spikes per session. 18 new tests, all DB-free. | `archive/091-106.md` |
| 106 | Documentation-integrity fix: decisions #98 and #99's own closing "Where it's built"/"Verified" paragraphs, found stranded at the tail of `confirmed-decisions.md` (discovered while appending #104/#105), restored to their rightful entries — moved, not reworded. Root cause of the original misplacement not determined. Same change rolled the file over: decisions #91–#106 archived to `archive/091-106.md`, fresh open file starts at #107. | `archive/091-106.md` |
| 107 | First Pullback and Reversal — design note locked (Stage 0, no code), the last two strategies from the planned v1 set (ORB, Gap, Volume Spike built). Both read `LevelInteractionEngine`'s touch/holding/rejected/conquered state via `get_snapshot()` (same singleton pattern as `get_market_clock()`), re-deriving rejected-vs-conquered one candle late from private per-symbol state rather than changing `base_strategy.py`'s signature or building unwired `on_event` Scheduler support — new open item D9. `allows_waiting` stays `False` for v1; First Pullback flagged as D5's natural first trigger, not built speculatively. Full walkthrough: `strategy-engine-design.md` §16. | `archive/107-121.md` |
| 108 | Design review incorporated before any strategy code was written. Found two real gaps vs. `LevelInteractionEngine`'s actual implementation (gap-through, cold-start-unknown-origin) — fixed in new shared module `level_touch_tracking.py`. Found a third, more serious issue independently: `get_snapshot()` fed by an async queue/worker, no way to detect a stale read — fixed by adding `last_applied_candle_ts` per entry (small additive change, backward-compatible). MATCH/SCORE boundary and invalidation framing tightened per review, `base_strategy.py` untouched throughout. Full walkthrough: `strategy-engine-design.md` §16, "What the design review changed." | `archive/107-121.md` |
| 109 | First Pullback built — `first_pullback_strategy.py`. Established trend + literally the first touch of `level_key` (default `vwap`) today + REJECTED resolution agreeing with trend direction. SCORE blends trend/volume/resolution-distance. Invalidation = `anchor_price`. 16 tests, real `EventBus`/`LevelInteractionEngine`/Postgres end-to-end. | `archive/107-121.md` |
| 110 | Reversal built — `reversal_strategy.py`. Established trend + level CONQUERED against it, any touch count (deliberately not gated to the first, unlike First Pullback). SCORE blends trend/volume/touch-count. Invalidation = `anchor_price`, falls back to the live level value for gap-throughs. Fires at most once per symbol per day. 12 tests + 13 pure tests for the shared `level_touch_tracking.py` module. Full suite: 486 passed, 0 failed. | `archive/107-121.md` |
| 111 | Gap/Volume Spike design review's six agreed changes made: `regular_open` now published by Feature Engine (no longer reconstructed in `gap_strategy.py`); Gap bounded to `max_minutes_since_open` (v1 default 60); Volume Spike gained `min_absolute_volume` (500) and `min_body_ratio` (0.3) checks; `_clamp`/threshold-guard/trend-magnitude extracted to new shared `scoring_utils.py`, adopted by ORB, Gap, Volume Spike, and — discovered duplicated a 4th/5th time via a mid-change `git pull` — First Pullback/Reversal too; `Opportunity.expected_horizon_minutes` added to `base_strategy.py`, populated by ORB/Gap/Volume Spike, left unpopulated for First Pullback/Reversal (that thread's own call). 19 new tests, zero regressions (346 passed vs. 327 baseline, same 40 pre-existing DB failures/119 skipped). | `archive/107-121.md` |
| 112 | Stage 2 (Strategy Scheduler/wiring) sequencing and scope locked (Stage 0, no code) — new D10. Renumbered from a collision at #111 (queued docs-only content never applied before #111 got taken by real code work — same category as the #98/#99 collision). Blocked until Momentum/VWAP land (still unconfirmed as of this entry); scope is wiring only (Scheduler + `OpportunityCreated` + read-side snapshot) — declarative `gate_conditions` enforcement and Opportunity Engine ranking explicitly deferred. Also fixes a stale §13 line claiming First Pullback/Reversal were unbuilt. | `archive/107-121.md` |
| 113 | Momentum and VWAP built — `momentum_strategy.py`/`vwap_strategy.py`, sixth and seventh v1 strategies, closing out the full planned set. Externally reviewed before code (same practice as #107/#108); three questions decided directly by Saqib: Momentum's `acceleration_score` threshold chosen from the formula's own verified semantics (no live-data calibration pass first); VWAP made disjoint from Reversal by construction (fires only in `trend_score`'s neutral band, direction from the actual resolved zone, never a mirror of trend); `ESTABLISHED_TREND_SCORE_THRESHOLD` promoted to `scoring_utils.py`, `reversal_strategy.py` refactored onto it (new D11: defaults match, not guaranteed to stay matched if either config is retuned independently). Momentum/ORB co-firing accepted, no arbitration added. Momentum's invalidation is a new private rolling swing lookback, not ATR/`LevelInteractionEngine`; both strategies' `structural_target` is the same mechanical R-multiple placeholder every other v1 strategy already uses (schema currently requires a float). 37 new tests (18 DB-free Momentum, 19 DB-gated VWAP) + 6 new `scoring_utils.py` tests, verified against a real locally-provisioned Postgres (not just DB-free) — 546 passed, 2 pre-existing failures confirmed unrelated against an untouched second clone. Stage 2 (D10) now unblocked. | `archive/107-121.md` |
| 114 | Stage 2, Track A built — `app/strategy_engine/scheduler.py` (`StrategyScheduler`). New open item D12: the live trigger is `MarketStateChanged`, not `FeaturesUpdated` as D10 originally planned — found by a real-engine integration test catching MarketStateEngine's debounced-worker async gap, not by inspection. Caches the real `FeaturesUpdated` payload for `FeatureSet` rather than reconstructing from `FeatureEngine.get_snapshot()` (which would silently drop OHLC). Exception isolation per strategy verified directly. `main.py`/`conftest.py` wired, flagged as a manual-merge point against Track B's parallel OpportunityCreated read-side work (not yet landed). 20 new tests incl. 2 real-engine end-to-end, verified against a real local Postgres — 565 passed, 3 pre-existing/unrelated failures (corrects decision #113's count of 2 — a third, order-sensitive failure confirmed on an untouched clone too), zero regressions. | `archive/107-121.md` |
| 115 | Track A/Track B collision at #114 reconciled. Both sessions independently logged their own delivery as #114 (neither could see the other's in-flight work); Track A's push overwrote Track B's decision-log record and left its `main.py`/`conftest.py` wiring unfinished despite Track A's own explicit flag to complete it — confirmed as a real, live bug by running the actual app lifespan (`OpportunityCache` never received a real `OpportunityCreated` publish). Fixed: `main.py`/`conftest.py` wired, re-verified by running (empty before, correctly populated after). D13 restored (`Opportunity` stays on `base_strategy.py`, imported directly — renumbered from a second collision, since Track A's own new D12 took that letter). D14 opened, not resolved: Track A's #114 says `active_from`/`active_to` enforcement is out of scope; Track B was separately told to use event timestamps for activation-window evaluation, implying enforcement was expected — a genuine conflict between what two sessions were told, flagged for Saqib rather than picked silently. No changes to `opportunity_cache.py`, `intelligence.py`, or `scheduler.py`. | `archive/107-121.md` |
| 116 | D14 resolved canonically (Stage 0, docs only, no code) — `active_from`/`active_to` enforcement confirmed OUT of scope for Stage 2, same treatment as `gate_conditions` (D10). The two apparently-conflicting instructions reconciled: "out of scope for now" is the architectural decision; "use event timestamps, never wall-clock now" is a future-implementation constraint that applies only if/when enforcement is later brought into scope — not present authorization to build it. `scheduler.py` unchanged (already correct). Stage 2 (both tracks) confirmed fully built and verified end-to-end: real Postgres full suite 577 passed, 1 pre-existing/unrelated failure; decision #115's "40 failed" figure confirmed to be a Postgres-availability artifact of that session's sandbox, not a regression. | `archive/107-121.md` |
| 117 | Declarative `gate_conditions` enforcement built (§2b) — new `app/strategy_engine/gate_conditions.py`, wired centrally into `scheduler.py` before `evaluate()`. Closes the item D10 deferred alongside `active_from`/`active_to` (`active_from`/`active_to` itself stays separately closed, D14/#116 — not reopened). v1 supports exactly `{"session": "regular"}` — confirmed by grep that all 7 real strategies declare exactly that and nothing else, and that no `vix` field exists anywhere to back §3's illustrative `vix_min` example. Real, currently-live gap closed for 3 of 7 strategies (First Pullback/Reversal/VWAP had zero session enforcement anywhere before this — the other 4 already self-gate inline, now redundant but untouched). Unrecognized `gate_conditions` fails loudly at Scheduler *construction* (`ValueError`), deliberately differing from the after_time/on_event "registered but unreachable" precedent (#114) — reasoning inline. 20 new tests (10 pure `gate_conditions.py` unit tests + 10 in `test_strategy_scheduler.py`, incl. 1 DB-gated real-engine end-to-end proving the exact failure mode against a real pre-market candle), verified against a real local Postgres — 598 total, zero regressions, same 2 pre-existing order/timing-sensitive failures as #114/#116, confirmed identical against an untouched second clone. | `archive/107-121.md` |
| 118 | Architectural rule made explicit: `StrategyConfig.gate_conditions` is declarative configuration; `StrategyScheduler` (via `gate_conditions.py`) is its sole enforcement authority — no strategy may independently interpret it or enforce its own gate. Raised directly by Saqib before calling #117's area finished. New open item **D16**: remove the 4 v1 strategies' pre-existing, now-redundant inline session checks (tolerated exception, not a template) — deferred, not done here. Docs/comments only, no behavior change — full suite re-run, 598 total, zero regressions, same 2 pre-existing failures as #117. | `archive/107-121.md` |
| 119 | D16 closed — standalone cleanup pass removing the v1 strategies' redundant inline session GATE checks, per decision #118's own trigger condition (Saqib's direct call, not a deferral). **Corrects D16's original "4 v1 strategies" framing to 3**: `gap_strategy.py`, `momentum_strategy.py`, `volume_spike_strategy.py` each had a removable `MarketClock.is_regular_session()` GATE check, now deleted (`clock = get_market_clock()` stays live in all three for `trading_day()`/`minutes_since_open()`); `orb_strategy.py` has no `is_regular_session()` call anywhere — its `minutes_since_open()` usage is load-bearing opening-range-formation MATCH logic, not a duplicate gate — and was left completely untouched, code-wise. Each file's `default_config()` comment and module docstring's "Session scope" section updated to point to `StrategyScheduler`/`gate_conditions.py` as sole enforcement (Gap/Volume Spike had a stale self-enforcement comment; Momentum did not, verified per-file rather than assumed uniform). Three `test_outside_regular_session_never_fires` tests removed, not rewritten — that contract now lives solely in `test_gate_conditions.py`/`test_strategy_scheduler.py`, which already proved it. MATCH/SCORE/PROPOSE confirmed byte-for-byte unchanged via diff against an untouched clone. `gate_conditions.py`'s/`scheduler.py`'s own docstrings corrected for the same 3-vs-4 imprecision. Full local Postgres suite (595 collected post-change, exactly 598 baseline − 3 removed): zero regressions; all 187 `strategy_engine`-related tests run 3x independently, 100% stable. Broader test-suite flakiness run down properly rather than glossed over — 2 more intermittent, order/timing-sensitive failures found beyond the previously-documented pair, both confirmed via repeated isolated reruns to also flicker on the untouched clone (Feature Engine cold-start/SMA-EMA-slope tests, no code path near `strategy_engine/`), so pre-existing and unrelated to this change. | `archive/107-121.md` |
| 120 | Performance Intelligence's persistence layer built — `strategy_outcomes` + `backtests` tables, §5/§7's shape (decision #89) implemented field-for-field. Establishes JSONB + native UUID PK/FK conventions (both confirmed absent beforehand, grepped). `feature_snapshot_id`/`opportunity_id` unenforced (target tables don't exist yet); `backtest_run_id` a real FK. Pydantic in new `schemas/performance.py`; ORM in `models/trading_intelligence.py` as `StrategyOutcomeRecord`/`BacktestRunRecord`. `record_strategy_outcome()` asserts `entry_qty == exit_qty` before any write, raises rather than swallowing (deliberate divergence from `MarketStateEngine._persist`'s soft-fail precedent). §5-vs-#98 nullability discrepancy recorded, not silently patched — new open item D17. Optional observability route intentionally skipped (sibling-track collision risk). No live caller wired — Execution Engine/Position Monitor don't exist. 8 new tests, real Postgres, 100% stable; 603 total post-change, same 2 pre-existing failures as #119, zero regressions. | `archive/107-121.md` |
| 121 | Decision-number collision at #120 reconciled (same category as #98/#99, #111/#112, #114/#115) — Opportunity co-occurrence/conflict view renumbered to #121, `app/trading_intelligence/opportunity_view.py` (new), a deliberately narrow, non-scoring read-side view over `OpportunityCache.get_snapshot()`. Per symbol, classifies 2+ currently-cached strategies as `agreement` (all one direction) or `conflict` (BUY and SELL both present, one entry covering every strategy involved) — mutually exclusive, never both. 0/1 cached opportunity is honestly absent from both. `confidence`/`setup_detected_at` are passthrough-only, never aggregated/compared/used to rank. Explicitly does NOT resolve D4 (`strategy-engine-design.md` §10) — D4 stays OPEN; unrelated to #120's own new D17. Terminology deliberately "currently cached," not "live," since `OpportunityCache` is status-blind with no TTL/purge, inherited as-is. Pure-function/thin-wrapper split (`compute_opportunity_conflicts()` / `get_opportunity_conflicts()`), mirroring `state_snapshot.py`'s cross-engine-read pattern — no new state, no singleton. Optional route intentionally skipped. Confirmed file-disjoint from #120's own footprint by diff. 16 new tests, `test_opportunity_view.py`, DB-free. | `archive/107-121.md` |
