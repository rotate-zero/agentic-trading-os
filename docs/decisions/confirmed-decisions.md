# Confirmed Decisions

**Extracted from:** `../architecture/system-design.md` §9 (original numbering preserved for continuity with any existing references).
**Companion documents:** [`../architecture/system-design.md`](../architecture/system-design.md) (how it's built), [`../architecture/trading-intelligence-architecture.md`](../architecture/trading-intelligence-architecture.md) (how it thinks), [`future-ideas.md`](./future-ideas.md) (deferred, not rejected), [`../roadmap/phase-roadmap.md`](../roadmap/phase-roadmap.md) (when things ship).

This is the running log of settled architecture decisions — the "why did we choose X" record, kept in one place instead of scattered as inline asides across growing documents. **New confirmed decisions get appended here, numbered sequentially, as part of the same change that makes them** — not batched up and written after the fact.

---

**A note on this file's structure (updated at decision #121's rollover):** this file holds only the MOST RECENT range of decisions — currently #122 onward. Decisions #1–#60 live in `archive/001-060.md`; decisions #61–#79 live in `archive/061-079.md`; decisions #80–#90 live in `archive/080-090.md`; decisions #91–#106 live in `archive/091-106.md`; decisions #107–#121 live in `archive/107-121.md`. All five moved verbatim and unedited at the point each rollover happened. See `README.md` in this directory for the full read/write/maintain protocol — every Claude session touching this project should read that file before writing a new decision here, not just this note.

---

122. **Performance Intelligence's read-side query layer built — `app/trading_intelligence/performance_queries.py`, decision #120's real GROUP BY queries over `strategy_outcomes`, §5.** Re-checked the decision-log tail immediately before writing — #121 was still latest, `confirmed-decisions.md` still empty of entries since its own rollover, so #122 was free, no collision to reconcile this time. File-disjoint from the sibling parallel track's own route/frontend work — confirmed by `diff -rq` against a freshly-pulled, untouched second clone: the only files touched are `app/trading_intelligence/performance_queries.py` (new), `backend/tests/test_performance_queries.py` (new), this entry, the matching `INDEX.md` row, a small §5 pointer note, and `TESTING.md`.

    **Placement: new sibling module, not a section inside `performance.py`.** `performance.py`/`test_performance_intelligence.py` (decision #120) stay completely untouched. `performance.py` is a write-time-invariant-then-persist concern; this module is read-only aggregation with no invariant to enforce and no single-row session lifecycle. Same "read-side view sits one layer above the thing it reads, as its own file" shape `state_snapshot.py` (decision #98) and `opportunity_view.py` (decision #121) already established in this codebase — followed here rather than invented fresh.

    **Two real queries, exactly the two with an unambiguous grouping key.** `get_win_rate_by_hour()` groups by `entry_filled_at` converted to `Settings.market_timezone` ("America/New_York" by default) — confirmed by grep that `core/market_clock.py`'s own `get_market_clock()` singleton already reads this same setting rather than a hardcoded literal, so this module reads it the same way instead of introducing a second, divergent source of truth for "which zone is market time." Bucket is `hour_et` (0-23, named explicitly so it can't be mistaken for a UTC hour) via real Postgres `entry_filled_at AT TIME ZONE :market_timezone` (DST handled by Postgres's own tzdata, no custom logic), `EXTRACT(HOUR FROM ...)`, cast to integer, ordered `hour_et ASC`. `get_expectancy_by_session_type()` groups by `context_at_entry->>'session_type'` (real Postgres JSONB `->>`, exercised against real Postgres in this build's own tests, not assumed from reading the code), ordered `session_type ASC`. `session_type` was chosen over the other two candidates §5's own field comment names ("gap day?, session type, VIX regime") for two checked reasons: decision #117 already confirmed by grep that no `vix` field exists anywhere in this codebase to back a VIX-regime grouping, and `gap_day` is a boolean flag, not a genuine multi-valued regime dimension the way `session_type` is. **This is Performance Intelligence's first concrete regime dimension, not "regime" solved generally** — no `regime_dimension` parameter, no generic JSONB-key abstraction; a second regime dimension would be a new function, not a parameter added to this one.

    **"Parameter sensitivity" is explicitly out of scope — no code, no stub, no narrowed version.** `StrategyConfig.params`'s shape varies per strategy (§3); there is no single column or JSON key to group by without a per-strategy params schema decision this task doesn't make.

    **Metrics defined exactly, not left implicit.** Both use `realized_r`, never `realized_pnl` (§5: R is size-comparable, raw PnL isn't). `realized_r` confirmed `NOT NULL` at both the ORM (`Numeric(10,4), nullable=False`) and Pydantic (`float`, no default, no `| None`) layers before writing any code — no NULL-population policy needed for either metric, and no test exercises one. Expectancy = `AVG(realized_r)` per group, computed in Postgres. Win rate = `win_count / total_trades`, where `win_count` is real Postgres `COUNT(*) FILTER (WHERE realized_r > 0)` — `realized_r <= 0`, including exactly `0`, is explicitly not a win. The division itself is done in Python, deliberately: Postgres `COUNT(*)` returns `bigint`, and `bigint / bigint` truncates there, so `COUNT(*) FILTER (...) / COUNT(*)` as one SQL expression would silently collapse every real ratio to `0` or `1`. Both operands are already fully reduced, one row per hour bucket, by the real `GROUP BY` — this is arithmetic on an aggregate result, not client-side row aggregation.

    **Live/backtest isolation enforced in SQL, not caller discipline.** `is_backtest: bool = False` on both functions is a strict either/or selector — `False` filters `is_backtest = false` (live only), `True` filters `is_backtest = true` (backtest only); there is no call shape that blends both populations in one result. §5/§7's "never blended with live in a live query" is a hard invariant, verified in this build's own tests by writing both live and backtest rows with deliberately different counts/results and confirming each query returns exactly one population's aggregate, never a mix.

    **Version filtering never silently blends strategies.** `strategy_version` without `strategy_name` raises `ValueError` — grepped first for an existing strategy_name+strategy_version filtering abstraction elsewhere in this codebase to reuse instead; found none, so this is new, narrowly-scoped validation, not a copy of a pre-existing pattern.

    **Empty-result semantics.** No matching rows -> `[]`, never `None`, a fabricated zero row, or a NULL-populated aggregate — `strategy_outcomes` has zero real production rows today, and this is the normal case, not an edge case.

    **Implementation shape.** Real SQL-level `GROUP BY` per query, never fetch-then-aggregate-in-Python — each public function is a thin wrapper around a private, pure `_build_*_query()` (returns a `Select`, no session) that opens its own `SessionLocal()`, executes, shapes rows into a local `@dataclass(frozen=True)` (`HourlyWinRate`, `SessionTypeExpectancy` — not new entries in `schemas/performance.py`, which this task leaves untouched), and closes the session, same lifecycle convention `record_strategy_outcome()` already uses in this same package. No caching, no persisted query results (§5's own "computed on demand" framing — persisting these would reintroduce the "stored rank" anti-pattern §5 opens by rejecting), no route (future work, sibling-track collision risk on `routes/intelligence.py`), no change to `StrategyOutcomeRecord`.

    **Tests: `backend/tests/test_performance_queries.py`, 8 new, real Postgres, all synthetic outcomes written via the real `record_strategy_outcome()` write path** (never a raw INSERT). Covers: empty table (both queries -> `[]`); win-rate-by-hour grouping + exact arithmetic across 2 hour buckets with win/loss/breakeven mixed; expectancy-by-session_type grouping + exact `AVG` across 2 session_type values, plus the honest-`None` group for a missing key; backtest isolation (mixed live/backtest rows, deliberately different counts so blending would be detectable, verified for both queries); strategy_version isolation (two versions of one strategy_name); strategy_name filtering; the `ValueError` guard for a version-only filter. Verified against real local Postgres 16 (provisioned directly in this session — `apt-get install postgresql`, `alembic upgrade head`), full suite run 3 times against a freshly wiped-and-recreated DB each time (this project's own established practice): 625-627 passed depending on run (the documented #119 flaky pair flickers as already described there), same two known tests each time (`test_vwap_publishes_even_while_sma_is_still_warming_up`, `test_daily_levels_carry_level_interaction_once_touched`), zero unexpected failures, zero regressions. This build's own touched area (`test_performance_queries.py` + `test_performance_intelligence.py`, 16 tests total) run 3x in isolation: 100% stable every time.

    **Docs updated alongside code, same change.** `strategy-engine-design.md` §5 gets a small pointer to this entry and module for the two query types now built — §5's own locked schema is untouched, and the pointer does not claim "expectancy by regime" or "parameter sensitivity" are solved beyond what's described above.

---

123. **Decision-number collision at #122 reconciled (same category as #98/#99, #111/#112, #114/#115, #120/#121) — read-side observability routes for `strategy_outcomes` (decision #120) and the opportunity co-occurrence/conflict view (decision #121), both of which explicitly skipped adding a route citing this exact parallel-track collision risk.** This session started against a pre-#122 clone; #122 (`performance_queries.py`) landed on `main` mid-session, discovered via a routine re-diff against a freshly-pulled tarball rather than assumed absent, at which point this session re-pulled live and rebased its own (file-disjoint, unaffected) work onto the current `main` before writing anything here. Re-checked the decision-log tail a second time, immediately before writing this entry — #122 still latest, no further collision.

    **Two new routes in `app/api/routes/intelligence.py`, additive only — `/state`, `/market-state`, `/context`, `/series`, and `/opportunities` are untouched.**
    - `GET /intelligence/strategy-outcomes` — `limit: int = Query(50, le=500)`, same `Query(default, le=cap)` shape `/series`'s `count` already uses (not `/opportunities`'s shape, which has no such param). Plain synchronous `SessionLocal()` read, `SELECT ... ORDER BY exit_filled_at DESC LIMIT :limit` — a raw recent-rows read, deliberately not an aggregate; does not import from, or depend on, #122's `performance_queries.py` in any way (confirmed both by not importing it and by this route's own tests passing before #122 existed in this session's working copy). Each ORM row is validated through the existing `schemas.performance.StrategyOutcome` (`model_validate(row, from_attributes=True).model_dump(mode="json")`) rather than a parallel response model — reuses decision #120's own paired contract instead of hand-mapping fields a second time. Response: `{"outcomes": [StrategyOutcome, ...]}`. `strategy_outcomes` has zero real rows in production today (no Execution Engine/Position Monitor writes to it yet — #120's own finding, still true) — an empty table returns `{"outcomes": []}`, 200, not an error; a dedicated test wipes the whole table and asserts exactly this (safe precisely because nothing else in this codebase writes to this table, live or in any other test's fixtures).
    - `GET /intelligence/opportunity-conflicts` — `symbol: str | None = Query(None)`, same optional-filter convention as `/opportunities`/`/market-state`/`/context`. A genuinely thin wrapper: `return get_opportunity_conflicts(symbol)`, no reshaping, no logic of its own — `opportunity_view.py` (decision #121) is completely untouched, and its own response shape (`{"agreements": {...}, "conflicts": {...}}`) is preserved exactly.
    - Both imports are local to their own function bodies, not hoisted to this file's top-of-file import block — same collision-avoidance precedent decision #114's own `/opportunities` route already set for exactly this file, extended here rather than reinvented.

    **Backend tests — `backend/tests/test_strategy_outcomes_and_opportunity_conflicts_routes.py`, 6 new, real Postgres.** `/strategy-outcomes`: a true empty-table case (whole table wiped, asserts `{"outcomes": []}` exactly — the literal current production state, not merely "our own rows absent"); a populated case inserting one `StrategyOutcomeRecord` directly via the ORM (not via `record_strategy_outcome()`, which belongs to #120/#122's own footprint) and asserting real returned field values (`outcome_id`, `direction`, `entry_price`/`exit_price`, `realized_pnl`/`realized_r`, `evidence`, `market_state_at_entry`, `trading_day`, `exit_filled_at`, ...) — not just status code or list length; `exit_filled_at DESC` ordering and `limit` capping, both using year-2099 synthetic timestamps so they're deterministically the newest rows regardless of anything else in the table (only the dedicated empty-table test needs literal table isolation). `/opportunity-conflicts`: real `EventBus`/`OpportunityCache` via `app.router.lifespan_context(app)` + httpx `ASGITransport` — the same "publish onto the real bus, hit the route on the same event loop" coordination `test_intelligence_routes.py` already established for `/state` — publishing real `OpportunityCreated` envelopes and asserting the route's actual `agreements`/`conflicts` shape, plus `symbol` filtering (a real BUY/SELL conflict for one ticker confirmed absent from a differently-filtered response, alongside a real agreement on a second ticker confirmed not leaking in either direction). Deliberately does not re-test `compute_opportunity_conflicts()`'s own classification rules (0/1-opportunity absence, malformed-direction exclusion, ordering) — that's `test_opportunity_view.py`'s job as of #121; this file only proves the route forwards correctly.

    **Frontend — `api-client.ts`, two new hooks, minimal UI surfacing, no new page/panel type.** `fetchStrategyOutcomes(limit?)`/`fetchOpportunityConflicts(symbol?)` added to `api-client.ts`, matching `fetchOpportunities`'s exact error-handling/URL-building convention, with `StrategyOutcomeWireShape`/`OpportunityAgreementWireShape`/`OpportunityConflictWireShape` types copied field-for-field from the real backend schemas (re-verified against `schemas/performance.py` and `opportunity_view.py`'s current contents, not assumed).
    - `useOpportunityConflicts.ts` — mirrors `useOpportunities.ts` exactly (fetch on mount + symbol change, `subscribeSymbol()`, refetch on the same `"opportunity.new"` WebSocket push), since it reads the same underlying `OpportunityCache` `useOpportunities` already watches. `useOpportunities.ts` itself is untouched.
    - `useStrategyOutcomes.ts` — deliberately does NOT copy that WS-push pattern. No event is published when a row is written to `strategy_outcomes` — confirmed by grep, no `OutcomeRecorded`-shaped `EventType` exists anywhere in `backend/app/schemas/events/`, because no live caller exists yet to publish one (Execution Engine/Position Monitor aren't built — #120). Subscribing to a channel that will never fire would be dead code dressed up as a refresh strategy. One-shot fetch on mount, `refetch()` exposed for whenever a real trigger exists later; no new manual-refresh button added, since no established equivalent affordance exists yet in the surrounding UI for one to match (Saqib's explicit call).
    - UI placement, Saqib's explicit call: `opportunity-conflicts` (symbol-scoped, same data source as `/opportunities`) surfaces as a small new section in `AIAnalysisPanel.tsx`/`InfoTab.tsx`'s `ConnectorContent` — same place `useOpportunities` already renders, `ConnectorContent` calling the new hook and passing `agreement`/`conflict` down as props, same "parent owns fetching, child stays presentational" split that file already establishes. `strategy-outcomes` (global, not symbol-scoped — the route itself has no `symbol` filter) surfaces instead in `InfoTab.tsx`'s `GeneralContent` (the market-wide view) as a new "Recent Closed Trades" section, rather than being duplicated under every connector's per-symbol panel. Empty state renders "No closed trades recorded yet" — always visible, never hidden, no fabricated rows. No new frontend page, route, panel type, or chart/visualization added.

    **Frontend tests: none added, matching existing practice** — no hook in this codebase has a companion test file today (confirmed by search, not assumed), so this task does not introduce a new frontend testing pattern solely for itself. `npx tsc -b`/`npx vite build` both clean, zero new errors — only the known pre-existing `GridPresetPicker` errors (decision #35), confirmed identical against a freshly-pulled untouched clone.

    **Verification.** Full backend suite against a real local Postgres 16 (provisioned directly in this session), before and after: before (untouched clone, post-#122), 627 collected, 624 passed, 3 of the #119-documented flaky-cluster tests failed that run; after (this build), 633 collected — exactly 627 + 6 new — 631 passed, 2 of the same #119 cluster failed, zero regressions. `diff -rq` against a freshly-pulled untouched second clone confirms the only files touched are: `backend/app/api/routes/intelligence.py` (extended), `backend/tests/test_strategy_outcomes_and_opportunity_conflicts_routes.py` (new), `frontend/src/services/api-client.ts` (extended), `frontend/src/hooks/useStrategyOutcomes.ts` (new), `frontend/src/hooks/useOpportunityConflicts.ts` (new), `frontend/src/components/ai-panel/AIAnalysisPanel.tsx` (extended), `frontend/src/components/workspace/InfoTab.tsx` (extended), this entry, the matching `INDEX.md` row, and `TESTING.md` — no accidental touch to `performance.py`, `performance_queries.py`, `models/trading_intelligence.py`, `schemas/performance.py`, `opportunity_view.py`, `useOpportunities.ts`, or any of the five untouched routes.

---

124. **Correction to decision #122's `get_expectancy_by_session_type()` — wrong JSONB extraction path, found during unrelated recon for a different task, fixed here.** Decisions are immutable once minted (this project's own standing rule) — #122's entry above is left exactly as originally written; this is a new, separate entry documenting a real implementation bug in the code #122 shipped, not a revision of #122's own text.

    **The bug, precisely.** `_build_expectancy_by_session_type_query()` extracted `context_at_entry->>'session_type'` — a flat top-level JSONB key. The REAL shape `context_at_entry` has in production is provider-keyed, not flat: `ContextEngine.get_snapshot()` (confirmed directly against `context_engine/engine.py`) returns `{"providers": {"calendar": {...}, "fundamentals": {...}, "news": {...}}, ...}` per symbol, and `state_snapshot.py`'s `capture_context_snapshot()` copies that `providers` dict verbatim as `context_at_entry`. There is no top-level `session_type` key anywhere in that real shape — the real field is `context_at_entry["calendar"]["session"]` (`CalendarProvider`'s own field name, confirmed against `context_engine/providers/calendar.py`). A second, compounding error: #122's own text and this module's original docstring described session values as "regular"/"pre_market"/"power_hour" — but the real `Session` enum (`core/market_clock.py`) has six values (`pre_market`, `open`, `lunch`, `power_hour`, `after_hours`, `closed`), and `"regular"` isn't one of them; the regular trading session is split across `open`/`lunch`/`power_hour`, not one unified label. Neither error was caught by #122's own tests, because `test_performance_queries.py`'s `_make_outcome()` fixture built `context_at_entry = {"session_type": "regular"}` directly — a shape and value matching the bug, not matching what `ContextEngine` actually produces. Against real production data (still zero rows today — no Execution Engine/Position Monitor exists to write one, same state since #120), this query would have silently returned only the honest-`None` group on every real call, forever — not a crash, a silently-useless result nobody would notice until they went looking for a real breakdown and never found one.

    **Fix.** `_build_expectancy_by_session_type_query()` now extracts `context_at_entry["calendar"]["session"].astext` — real chained JSONB indexing (`->'calendar'->>'session'`), exercised against real Postgres. Kept the output field/function name as `session_type` — a fine, still-accurate label for the concept; only the extraction path and the module's own docstring narrative were wrong, not the name. `test_performance_queries.py`'s `_make_outcome()` rebuilt to nest `context_at_entry` the same way `ContextEngine` really does (`{"calendar": {"session": ...}}`), and its synthetic session values corrected from the fictional `"regular"` to the real `"open"` (the closest single real value to what "regular" was meant to convey — no new "collapse open/lunch/power_hour into one regular bucket" behavior added; that would be a real, separate design decision this correction does not make, and the finer six-value granularity the real data actually has is arguably more informative, not a loss). One new regression test added, `test_expectancy_by_session_type_reads_the_real_provider_nested_shape` — a direct ORM insert (not via `_make_outcome()`, so a shape bug in that helper can't hide a shape bug in the query the same way twice) with a realistic `context_at_entry` carrying sibling `fundamentals`/`news` keys alongside `calendar`, proving the query reads `calendar.session` specifically. 9 tests total in this file now (8 original + 1 new), all passing against real Postgres, 100% stable across 3 isolated reruns.

    **Scope discipline — did not expand beyond the bug.** Did not add a `regime_dimension` parameter, a session-bucket-collapsing option, or any handling for `gap_day`/`vix_regime` — all explicitly out of scope for #122 originally and still out of scope here; this entry fixes exactly the one wrong JSONB path and the test fixture that hid it, nothing else. `performance.py`, `models/trading_intelligence.py`, `schemas/performance.py`, `app/api/routes/intelligence.py`, and every frontend file are untouched — this correction's footprint is exactly `performance_queries.py` and `test_performance_queries.py`.

    **Verification.** Full backend suite against real local Postgres, before/after: before (untouched clone, current `main`, post-#123), 633 collected, 631 passed, 2 of the #119-documented flaky cluster failed; after, 634 collected — exactly +1, the new regression test — 632 passed, the same 2 #119-cluster tests failed both times, zero regressions. `diff -rq` against a freshly-pulled untouched clone confirms only `performance_queries.py`, `test_performance_queries.py`, this entry, the matching `INDEX.md` row, and `TESTING.md` differ.

---

125. **Context Engine surfaced in the UI for the first time — `CalendarProvider`/`FundamentalsProvider`/`NewsFlagProvider` (decision #90/#96) were fully built, wired, and reachable via the already-existing `GET /intelligence/context` (decision #98) but invisible anywhere in the frontend until this task. Same "a real, built capability exists but nothing shows it" gap decision #123 closed for `strategy_outcomes`/opportunity conflicts — frontend-only this time, zero backend files touched.** Re-checked the decision-log tail twice — once before reading, once immediately before writing this entry, via a full fresh re-pull diffed against this session's own working copy — #124 stayed latest both times, no collision to reconcile.

    **No backend change, including the route itself.** `GET /intelligence/context` (confirmed decision #98) was re-read, not re-verified-by-modifying — `app/api/routes/intelligence.py`, `context_engine/engine.py`, and all three provider files are byte-for-byte untouched, confirmed by `diff -rq` against a freshly-pulled clone.

    **Wire shapes confirmed against the real provider code, not guessed from any prior description.** `ContextEngine.get_snapshot()`'s own envelope — `{"global": {"providers": {...}, "evaluated_at": iso|null}, "symbols": {ticker: {"providers": {...merged...}, "evaluated_at": iso}}}` — matches exactly what `engine.py`'s own docstring and implementation show; no discrepancy found here (unlike decision #124's finding for a *different* consumer of this same provider data, `context_at_entry` in `strategy_outcomes` — that shape is `state_snapshot.py`'s own separate flattening, not `get_snapshot()`'s envelope, and the two were not conflated). Per-provider payloads copied field-for-field from the live files: `CalendarProvider.evaluate()` (`session`/`is_market_open`/`is_half_day`/`minutes_since_open`/`fed_day`/`trading_day` — `minutes_since_open` confirmed always `int`, never `null`, by reading `MarketClock.minutes_since_open()` directly, not assumed); `FundamentalsProvider.evaluate()` (all twelve fields, `sector` confirmed permanently `null` in this build per that file's own docstring — Finnhub's `/stock/profile2` has no separate sector field); `NewsFlagProvider.evaluate()` (`present`/`count_15m`/`recency_seconds`/`importance`, `present: false` unconditionally for SPY/QQQ/IWM per decision #94). New types in `api-client.ts`: `CalendarProviderWireShape`, `FundamentalsProviderWireShape`, `NewsProviderWireShape`, `ContextProvidersWireShape`, `ContextGlobalWireShape`, `ContextSymbolWireShape`, `ContextSnapshotWireShape`, plus `fetchContextSnapshot(symbol?)` matching `fetchOpportunities`'s exact error-handling/URL-building convention.

    **Aggregate/global context score — checked directly, confirmed absent, nothing added.** `ContextEngine.get_snapshot()`'s "global" section is exactly `{"providers": {...}, "evaluated_at": ...}` — no aggregate score, composite assessment, or other derived/summary field exists anywhere in the real snapshot (confirmed against `engine.py`'s own docstring and implementation, not inferred). Per this task's own explicit instruction not to recompute, reinterpret, or invent one if absent: none was added, client-side or otherwise. Each Calendar field is surfaced as-is.

    **New hook, `useContextSnapshot.ts` (named to avoid colliding with React's own `useContext`) — fetch-based, NOT WebSocket-push, and lightly polled.** No `EventType.CONTEXT_CHANGED` entry exists in `EVENT_TO_CHANNEL` (`app/api/websocket/channels.py`, confirmed against the live file) — no channel relays Context updates to the frontend today, so there is nothing to subscribe to, unlike `useOpportunities.ts`/`useOpportunityConflicts.ts`. Unlike `useStrategyOutcomes.ts`'s own no-WS case (`strategy_outcomes` has no live writer at all yet, so a bare fetch-on-mount is the honest choice there), Context genuinely changes on its own while a panel stays open — session-boundary transitions several times a trading day plus a 15-minute Fundamentals/News timer per symbol (both cadences read directly from `engine.py`'s own module docstring, decisions #92/#96) — so this hook adds a light 60-second poll on top of fetch-on-mount/fetch-on-symbol-change. Reasoning for the specific interval, stated rather than defaulted to silently: `get_snapshot()` is a synchronous, in-memory, zero-I/O read by its own docstring, so a light poll costs effectively nothing server-side; 60s is tighter than the 15-minute provider cadence (most ticks return byte-identical data, which is fine given the read is free) but loose enough not to hammer anything, and it catches a session-boundary transition within a minute without the client needing to compute its own guess at exactly when the next boundary lands. Uses a `mountedRef` guard rather than the single per-effect `cancelled` closure `useOpportunities.ts`/`useStrategyOutcomes.ts` use, since `load()` here fires repeatedly within one effect run (initial call plus every poll tick), not just once — a single closed-over flag from the first call wouldn't cover later ticks.

    **UI placement — Calendar vs. Fundamentals/News is a genuine split, resolved here rather than picked arbitrarily.** Calendar's fields (`session`/`is_market_open`/`fed_day`/...) are market-wide, not symbol-specific — same scope `InfoTab.tsx`'s `GeneralContent` already has — so a new `MarketSessionSummary` section surfaces there, placed right after the market-overview rows and before decision #123's own "Recent Closed Trades" section; always rendered, including the not-yet-evaluated case, same "don't hide it, don't fabricate it" posture that section already established. Fundamentals/News are genuinely per-symbol (decision #96) — a new `SymbolContextSummary` section surfaces instead in `AIAnalysisPanel.tsx`, alongside decision #123's own `ConflictStatus`, fed by `ConnectorContent`'s own new `useContextSnapshot(symbol)` call (same "parent owns fetching, child stays presentational" split that file already establishes; `AIAnalysisPanel` itself still fetches nothing). One real deviation from #123's own layout, called out rather than silently copied: `ConflictStatus` renders `null` when neither prop is set, because 0/1 cached opportunities is a legitimate "no classification applies" case — but `SymbolContextSummary` never renders `null`, and is deliberately hoisted into a shared `header` block rendered from all three of `AIAnalysisPanel`'s return branches (loading/empty/populated), not only the populated one — Context Engine data has nothing to do with whether Strategy Engine has fired an opportunity yet, so gating Fundamentals/News behind the Opportunities-empty check (the shape #123's `ConflictStatus` placement quietly inherited, since it genuinely IS opportunity-derived) would have hidden real, available data in the common early-session case where a symbol has context but no opportunities yet.

    **Empty/absent state, rendered honestly, not hidden.** Fundamentals: a `null` fundamentals entry (Context Engine never evaluated this symbol) and a non-null entry with every field `null` (evaluated, but no `symbol_fundamentals` refresh has landed — `fundamentals.py`'s own `_read` docstring) render the same "No fundamentals data yet." — the distinction isn't actionable from the UI, so one honest message covers both rather than manufacturing a difference. `sector` is never displayed at all (not even as `—`): it's permanently `null` by construction in this build, not a value that's ever "not yet fetched," so showing it would only ever read as dead placeholder text. News: `null` (never evaluated) renders "No news data yet."; `present: false` (evaluated, nothing found) renders "No recent headlines." — deliberately the same neutral copy whether that's a genuine no-headlines symbol or SPY/QQQ/IWM's unconditional decision-#94 exclusion, since the wire shape is identical either way and this UI has no honest way to distinguish them.

    **Scope discipline.** No new page, route, panel type, or chart/visualization added — this is two new sections inside two already-existing views. `useOpportunities.ts`, `useOpportunityConflicts.ts`, and `useStrategyOutcomes.ts` are untouched; the new hook sits alongside them, not merged into any of the three.

    **Tests.** No companion test file for `useContextSnapshot.ts` — re-confirmed by search immediately before writing this entry that no hook in this codebase has one today (same practice decision #123 already documented, still true), so this task doesn't introduce a new frontend testing convention solely for itself. `npx tsc -b` and `npx vite build` both clean — zero new errors, only the four known pre-existing `GridPresetPicker` errors (decision #35), confirmed identical (same four, same lines) against a freshly-pulled untouched second clone before filtering.

    **Verification.** `diff -rq` against a freshly-pulled untouched second clone confirms the only files touched are: `frontend/src/services/api-client.ts` (extended), `frontend/src/hooks/useContextSnapshot.ts` (new), `frontend/src/components/workspace/InfoTab.tsx` (extended), `frontend/src/components/ai-panel/AIAnalysisPanel.tsx` (extended), this entry, the matching `INDEX.md` row, and `TESTING.md` — zero backend files, zero unrelated frontend files (no touch to `useOpportunities.ts`, `useOpportunityConflicts.ts`, `useStrategyOutcomes.ts`, `websocket-client.ts`, `channels.py`, or any router/page file).

---

126. **`EventType.CONTEXT_CHANGED` wired to a real WebSocket channel, closing the exact gap decision #125 documented and left open — `useContextSnapshot.ts` switched from poll-primary to WebSocket-primary with the poll retained as a safety net.** `ContextEngine.evaluate_all()`/`evaluate_for_symbol()` already published `ContextChanged` (decisions #92/#96) — confirmed by direct read that this was genuinely still true before touching anything, not assumed from #125's own text. The gap was exactly one missing entry in `EVENT_TO_CHANNEL` (`app/api/websocket/channels.py`), not a missing publish call. Re-checked the decision-log tail twice — once before reading, once immediately before writing this entry, via a full fresh re-pull diffed against this session's own working copy — #125 stayed latest both times, no collision to reconcile. Mid-task, a separate parallel session's Backtest Runner Unit 4 landed on `main` (`app/backtest_runner/runner.py`, `tests/test_backtest_runner.py`, its own `CHANGES.md` entry, no decision-log entry of its own yet) — confirmed file-disjoint from this task's own footprint by re-diffing against a fresh pull mid-session, and this session's own working tree was rebased onto that updated `main` before finishing, rather than shipping against the now-stale pre-Unit-4 snapshot it started from.

    **Channel string: `"intelligence.context"`.** Follows the existing convention `LEVEL_INTERACTION_CHANGED: "intelligence.level"` already set — `intelligence.*` for Trading Intelligence-layer events pushed to the frontend, channel name matching the event's own semantic domain rather than its literal type name. Searched the whole frontend tree for the literal string `intelligence.context` before locking it in (not merely for a JSON label matching it) — zero references anywhere, so no conflicting subscription/meaning existed to collide with. One channel serves both of `ContextChanged`'s envelope shapes (decision #96) — market-wide (`symbol` unset) and per-symbol (`symbol` set) — same "one channel, distinguish by envelope fields" convention `MarketStateChanged` already uses; a second channel per shape was considered and rejected as an unjustified new routing mechanism for what the frontend already treats as one logical topic.

    **No backend logic added beyond the one dict entry.** `ContextEngine`'s publish side, `ContextChanged`'s payload schema (`providers: dict[str, dict]`), and every other `EVENT_TO_CHANNEL` entry are untouched — confirmed by `diff -rq` against a freshly-pulled clone. This was deliberately scoped as a routing fix, not an opportunity to touch the engine that happened to be adjacent.

    **Frontend: WebSocket push as an invalidation signal, not a second normalization path.** `useContextSnapshot.ts`'s `onUpdate` handler calls the hook's own existing `load()` (the real `GET /intelligence/context` fetch, decision #98) rather than hand-parsing `WireMessage.payload` into `CalendarContext`/`FundamentalsContext`/`NewsContext` a second time — the same re-fetch-on-push choice `useOpportunities.ts` already made, for the same reason: a second, parallel merge-the-payload-into-state implementation would need to be kept in sync with `get_snapshot()`'s own dict-shaping logic indefinitely, for a read `get_snapshot()`'s own docstring already establishes is free (synchronous, in-memory, zero-I/O). `UseContextSnapshotResult` and all three normalized types are byte-for-byte unchanged; every consumer (`InfoTab.tsx`'s `MarketSessionSummary`, `AIAnalysisPanel.tsx`'s `SymbolContextSummary`) needed zero changes, confirmed by reading both before finishing rather than assumed from the hook's own interface alone.

    **Symbol filtering, and why it's necessary rather than optional.** `ContextChanged`'s two shapes share one channel, so an unfiltered handler would refetch on every push regardless of relevance — including a different symbol's Fundamentals/News changing while this hook instance displays another ticker entirely. Routing rule: `symbol == null` (global Calendar changed) always reloads, since Calendar is part of every hook instance's returned shape regardless of the `symbol` argument; `symbol === this hook's own symbol argument` reloads, since that's this instance's own Fundamentals/News; any other symbol is ignored outright, never merged in. Filtering reads through a `symbolRef` (already present in the hook for the same reason `useOpportunities.ts` uses one) rather than the `symbol` argument directly, so a push arriving after a symbol switch — but before the new effect's own closures are established — can't act on a stale captured value.

    **Poll retained as a safety net, not removed — reasoning specific to this hook, not copied from `useOpportunities.ts`/`useOpportunityConflicts.ts` (neither of which keeps one).** `OpportunityCreated` fires on every qualifying candle during a live session, frequently enough that a dropped/reconnecting WebSocket self-heals quickly once reconnected. Context changes on a materially slower cadence — session boundaries a handful of times a trading day plus a 15-minute Fundamentals/News timer per symbol (`engine.py`'s own module docstring, decisions #92/#96) — and `channels.py`'s `websocket_endpoint` pushes no snapshot on (re)subscribe, confirmed by reading it: it only acks. A session that drops and reconnects therefore has no self-correcting signal until the next real `ContextChanged` fires, which could be up to 15 minutes away, unlike Opportunities' much shorter natural gap. Given `get_snapshot()` costs nothing server-side (same zero-I/O property decision #125 already established), keeping the poll running underneath the push bounds that worst case for effectively zero cost, rather than reintroducing an open-ended staleness window purely for architectural symmetry with hooks whose underlying data changes on a different timescale. `POLL_INTERVAL_MS`'s own comment and the hook's top-level docstring were both rewritten to state this — the old comment's "no channel exists" framing was the one piece of #125's reasoning this task genuinely overturned, and the new comment says so rather than leaving stale text next to new behavior.

    **Architecture docs — checked directly rather than assumed from the task prompt's own framing.** Grepped `system-design.md` and `trading-intelligence-architecture.md` for `EVENT_TO_CHANNEL` before writing anything: neither document actually described the mapping as incomplete, or mentions `EVENT_TO_CHANNEL` at all — `system-design.md` §4.12's WebSocket Gateway section lists a handful of illustrative channel names only (doesn't include already-built channels like `intelligence.level` either), and `trading-intelligence-architecture.md` doesn't describe WebSocket routing at all. The actual "no channel exists yet" claim lived only in this file's own decision #125 entry, not in either architecture doc — so no stale claim needed correcting there, and no unrelated rewrite was manufactured to justify a documentation change this task didn't actually need. The one genuinely relevant, already-existing table row — `system-design.md` §10.3's `ContextChanged` entry — got one added sentence noting the new channel and this decision number, the smallest change that keeps that row accurate; no new section, no diagram (this is a four-node linear flow already covered in prose the same way §5's Tick→Chart walkthrough covers a structurally identical one — a diagram here would decorate, not clarify).

    **Tests — new `backend/tests/test_websocket_channels.py`, not folded into `test_event_bus.py`.** `test_event_bus.py` tests the Bus's own dispatch mechanics and has no concept of WebSocket; this task tests the Gateway's `EVENT_TO_CHANNEL` routing table plus real client delivery, a different unit under test — same "one file per module under test" convention `test_opportunity_view.py`/`test_performance_queries.py` already establish. Real routing/delivery, not a mocked `ConnectionManager`: `TestClient(app)` as a context manager runs the real lifespan (`conftest.py`'s own established convention), and `client.portal` (an `anyio` `BlockingPortal`, populated by `TestClient.__enter__`) runs `bus.publish()` on the same event loop the app and the WebSocket session are running on — the supported mechanism for this, confirmed by reading the installed Starlette source directly rather than assumed, and avoiding the exact "Queue bound to a different event loop" failure mode decision #38 already found. Four tests: the `EVENT_TO_CHANNEL` mapping itself (regression guard); a global (`symbol` unset) publish reaching the channel; a per-symbol publish reaching the channel with `symbol` set; channel isolation (an unrelated `DevPing`, routed to `"dev.ping"`, never reaching a socket subscribed only to `"intelligence.context"`). A real, unanticipated finding surfaced while writing these: `ContextEngine.start()`'s global loop AND its per-symbol bootstrap loop both fire an evaluation immediately on startup (`engine.py`'s own "fire once immediately" comments) — so entering `TestClient(app)` against the real migrated database genuinely publishes one real `ContextChanged(symbol=None)` plus one real `ContextChanged(symbol=<ticker>)` per row in `scanner_universe_symbols` (6 real seeded rows, migration 0004 — AAPL/MSFT/NVDA/AMD/TSLA/SPY, confirmed by querying the migrated DB directly), all landing on this same new channel before or interleaved with anything this task's own tests publish. A naive "the next message is mine" assertion was flaky against this — same category of problem `test_intelligence_routes.py`'s own `_wait_until_published()` helper already exists to solve for a different engine's real concurrent activity. Resolved the same way: a bounded "keep reading until a message matches" loop (count-bounded, not a receive-level timeout, since `receive_json()` has no public per-call timeout without reaching into `WebSocketTestSession`'s private `_send_rx`), plus a synthetic never-seeded ticker for the per-symbol test and a distinctive payload marker for the global test, so real startup traffic can never coincidentally satisfy either test's predicate. No database write happens anywhere in this feature itself (Event Bus → WebSocket Gateway has zero persistence) — the real Postgres connection present in these tests is inherited from the app's own real lifespan (`ContextEngine`'s bootstrap reading `scanner_universe_symbols`), not something this feature's own code path needs.

    **Full-suite validation, run against current `main` (post-Backtest-Runner-Unit-4), not this session's own stale starting snapshot.** Backend: 669 collected on a freshly-pulled current-`main` baseline, 673 after this task's own 4 new tests (exactly +4). Same pre-existing flaky cluster both before and after (decision #119) — across six baseline runs and three working-tree runs, every failure seen was one of `test_vwap_publishes_even_while_sma_is_still_warming_up`, `test_daily_levels_carry_level_interaction_once_touched`, `test_sma_ema_slope_family_groups_under_the_owning_period_and_is_excluded_from_level_interaction`, or `test_feature_engine_backfills_from_persisted_history_on_cold_start` — the last of these reproduced on the untouched baseline too once enough runs were taken, not something this task introduced; zero new failures, zero regressions. Frontend: `npx tsc -b` and `npx vite build` both clean on the working tree, identical to a freshly-pulled current-`main` baseline — only the four known pre-existing `GridPresetPicker` errors (decision #35), same lines, in both.

    **Verification.** `diff -rq` against a freshly-pulled untouched clone of current `main` confirms the only files touched are: `backend/app/api/websocket/channels.py` (one added dict entry + comment), `backend/tests/test_websocket_channels.py` (new), `frontend/src/hooks/useContextSnapshot.ts` (extended), `docs/architecture/system-design.md` (one added sentence in §10.3's `ContextChanged` row), this entry, the matching `INDEX.md` row, and `TESTING.md`. Confirmed untouched: `backend/app/backtest_runner/` and `backend/tests/test_backtest_runner.py` (the parallel session's own files, present because this task rebased onto current `main`, not modified by this task), `backend/app/api/routes/intelligence.py`, `backend/app/trading_intelligence/performance_queries.py`, `useOpportunities.ts`, `useOpportunityConflicts.ts`, `useStrategyOutcomes.ts`, `InfoTab.tsx`, `AIAnalysisPanel.tsx`, and `api-client.ts`.

---

127. **`performance_queries.py`'s two `GROUP BY` queries (#122, correction #124) exposed end-to-end — `GET /intelligence/win-rate-by-hour`, `GET /intelligence/expectancy-by-session-type`, `usePerformanceAnalytics.ts`, and a new "Strategy Performance" section in `InfoTab.tsx`'s `GeneralContent`.** Closes the exact gap #122's own entry named as deferred future work ("no route... would risk colliding with the sibling parallel track's own route work on `routes/intelligence.py`") — the same category of deferred-then-landed route #120/#121 waited on before #123 closed it. Re-checked `INDEX.md`/`confirmed-decisions.md`'s tail three times across this task (before reading, mid-task, immediately before writing this entry) — #126 stayed latest throughout, no numbering collision to reconcile.

    **A stale instruction, corrected against the real code rather than followed.** A draft of this task's own prompt asserted that `get_win_rate_by_hour()`/`get_expectancy_by_session_type()` support only `strategy_name`/`strategy_version`, and warned against "inventing" `is_backtest` as a filter. Read directly against `performance_queries.py`'s real, current signatures before writing any route, this is false: `is_backtest: bool = False` is both functions' own third keyword argument, already fully implemented as a strict live/backtest selector enforced in SQL (`_common_filters()`), documented in both functions' own docstrings. Kept as a real, exposed filter on both routes — dropping it to match a stale assumption would have meant deliberately hiding an already-built, already-correct capability. Flagged here per this project's own "follow the code, document the discrepancy" standing rule, not silently corrected without a trace.

    **Route placement: appended to `intelligence.py`, not a new file — same file #120/#121/#122 all deferred a route into out of caution, and #123 already proved the append-only-at-file-end pattern survives that exact risk once.** A dedicated route file was considered and rejected: it would need `main.py` wired for registration too (`app.include_router(...)`), which is itself a documented collision point (#114/#115) — moving the seam to `main.py` isn't materially safer than the seam `intelligence.py` already has a proven pattern for, and the docs directly instructed against touching `main.py` for this reason. Two new routes, both using the same "local import, single additive block at file-end, thin wrapper" shape #123's own two routes established: `GET /win-rate-by-hour` and `GET /expectancy-by-session-type` (one route per underlying query function, matching that module's own two-functions granularity rather than merging both into one combined response).

    **Wire shapes, verified against the real dataclasses, not guessed.** `HourlyWinRate` -> `{hour_et: int, total_trades: int, win_count: int, win_rate: float}`; `SessionTypeExpectancy` -> `{session_type: str | None, trade_count: int, expectancy_r: float}` — both read directly from `performance_queries.py`'s current field definitions. Routes return `{"hourly_win_rates": [...]}` / `{"session_expectancy": [...]}`, each entry a plain `dataclasses.asdict()` of the real row — no new Pydantic schema (matching that module's own stated reasoning for using local dataclasses over `schemas/performance.py`), no reshaping, no route-level recomputation. `strategy_version` without `strategy_name` (`_validate_strategy_filters()`'s own guard) becomes a 400 via `HTTPException`, same clean-400 posture `GET /series` already uses for an unsupported timeframe.

    **Filter surface: exactly the three real parameters, nothing invented.** `strategy_name`, `strategy_version`, `is_backtest` — verified against `get_win_rate_by_hour()`/`get_expectancy_by_session_type()`'s own signatures before writing either route or wire type. No date filter, symbol filter, or pagination exists on either function, so none is exposed.

    **Frontend placement: `InfoTab.tsx`'s `GeneralContent`, directly below `RecentClosedTrades` — same reasoning #123 already used for that section.** Both queries are strategy/performance-level, not symbol-specific (neither route has a `symbol` filter) — the same "global data belongs in the market-wide view, not per-connector `ConnectorContent`" placement rule #123/#125 both already applied. No new dashboard, page, tab, or chart — one compact section, two subsections ("Win rate by hour," "Expectancy by session"), styled with this file's own existing row/border/font-mono conventions. No filter UI (strategy/backtest dropdowns) added — an explicit, stated call, not an oversight: this is minimal surfacing of an existing capability, and both routes stay fully filterable for a future, separately-considered UI.

    **Hook design: one combined hook, `usePerformanceAnalytics.ts` — verified no existing hook already owned this responsibility before adding it.** Fetches both endpoints (via `Promise.all`) and exposes one combined `{winRateByHour, sessionExpectancy, loading, error, refetch}` state, since both back a single UI section with one loading/error state; the two underlying fetches remain two separate wrapper functions in `api-client.ts`, matching the query layer's own two-functions granularity. Deliberately one-shot on mount, not polled and not WebSocket-driven — no event is published anywhere when a `strategy_outcomes` row is written (confirmed by grep, same finding #123's own entry already made for `useStrategyOutcomes.ts`), and these two routes are aggregate reads over that same table, so there is equally no event here to subscribe to. `refetch()` exposed for the same "whenever a real trigger eventually exists" reason `useStrategyOutcomes.ts`'s own `refetch()` is.

    **A deliberate, first-of-its-kind deviation from every existing hook's shape: an explicit, caller-visible `error` state, distinct from genuinely empty data.** `useStrategyOutcomes.ts`, `useOpportunityConflicts.ts`, and `useContextSnapshot.ts` were all re-read before writing this hook — none of the three exposes an error field; all three collapse a fetch failure into `console.error` plus an empty/default result, indistinguishable from "genuinely no data." That collapsing is exactly wrong for this section specifically: `strategy_outcomes` legitimately has zero real rows today (no Execution Engine yet), and a backend outage must not render identically to that honest empty state. `winRateByHour`/`sessionExpectancy` are only ever populated on a genuinely successful response; a failed fetch clears both to `[]` and sets `error` to the `ApiError` message instead, and `InfoTab.tsx`'s new section renders three distinct states (loading / error / "No performance data yet.") rather than two.

    **Empty-state behavior.** Both routes honestly return `{"hourly_win_rates": []}` / `{"session_expectancy": []}` — 200, not an error — when nothing matches, since `strategy_outcomes` has zero real production rows today. No fabricated zero-row chart, no invented placeholder number, no synthetic bucket. Confirmed the frontend section renders "No performance data yet." only in this genuinely-empty case, never for a request error (see the `error`-state deviation above).

    **Parallel-work safety — a real, unplanned rebase, not just a routine check.** A fresh re-pull partway through this task surfaced a genuinely new parallel landing on `main`: Backtest Runner "Unit 5" (test-only, its own `CHANGES.md`, one new file `backend/tests/test_backtest_runner_regression.py`, 20 tests) — confirmed via `diff -rq` that `backend/app/backtest_runner/` is byte-identical to what this task started from and that `intelligence.py` upstream carries zero backtest-related content, i.e. genuinely file-disjoint, the same situation decision #126 already documented once for Unit 4. Pulled that one test file into this session's working tree purely so this entry's own before/after counts reflect current `main`, not this session's stale starting snapshot — it is NOT part of this task's own delivered footprint (it already exists on `main`; Saqib's checkout already has it) and is not included in this delivery's zip. `CHANGES.md` (also updated by that same Unit 5 delivery) is likewise left untouched by this task — not this task's file to overwrite.

    **Tests.** New `backend/tests/test_performance_analytics_routes.py`, 9 tests, real Postgres — mirrors `test_strategy_outcomes_and_opportunity_conflicts_routes.py`'s own route-test posture (direct `StrategyOutcomeRecord` ORM inserts, proving the route forwards to and shapes the real query-layer result, not re-testing the grouping/arithmetic `test_performance_queries.py` already covers exhaustively): empty case (both routes), populated case asserting exact `hour_et`/`total_trades`/`win_count`/`win_rate` and `session_type`/`trade_count`/`expectancy_r` values including the honest-`None` session group, `strategy_name` filtering, `is_backtest` forwarding (mixed live/backtest rows, deliberately opposite outcomes so blending would be detectable), and the `strategy_version`-without-`strategy_name` 400 for both routes. 100% stable across isolated reruns. No new frontend tests, matching this codebase's existing test-free hook convention (#123/#125/#126 all note the same).

    **Verification.** Baseline against a freshly re-pulled current `main` (including Backtest Runner Unit 5's own 20 new tests, per the rebase above): 693 collected, 691 passed, the same 2 #119-documented flaky-cluster tests failed (`test_vwap_publishes_even_while_sma_is_still_warming_up`, `test_daily_levels_carry_level_interaction_once_touched`). Working tree: 702 collected, 700 passed — exactly +9, this task's own new tests — same 2 flaky-cluster tests failed, zero regressions. Full suite re-run twice against a freshly wiped-and-recreated local Postgres 16 (provisioned directly in this session — `apt-get install postgresql`, `alembic upgrade head`), identical result both times. `npx tsc -b`/`npx vite build` both clean, only the four known pre-existing `GridPresetPicker` errors (decision #35), same lines, confirmed against a freshly-pulled untouched clone before attributing them. `diff -rq` against that same untouched clone confirms the only files this task touched are: `backend/app/api/routes/intelligence.py` (extended), `backend/tests/test_performance_analytics_routes.py` (new), `frontend/src/services/api-client.ts` (extended), `frontend/src/hooks/usePerformanceAnalytics.ts` (new), `frontend/src/components/workspace/InfoTab.tsx` (extended), `docs/architecture/strategy-engine-design.md` (§5's diagram extended, one new paragraph, one new Stage 12 checklist entry), this entry, the matching `INDEX.md` row, and `TESTING.md` — no accidental touch to `performance_queries.py`, `models/trading_intelligence.py`, `schemas/performance.py`, `useStrategyOutcomes.ts`, `useOpportunityConflicts.ts`, `useContextSnapshot.ts`, `AIAnalysisPanel.tsx`, `main.py`, `backend/app/backtest_runner/`, `backend/app/context_engine/`, or `backend/app/api/websocket/channels.py`.

---

128. **Backtest Runner v1 closed out — as-built record of `backend/app/backtest_runner/` (Units 1-5, five separate sessions/deliveries), the first real writer of `strategy_outcomes`/`backtests` (decision #120's tables, waiting since then). Decision-number collision at #127 reconciled (same category as #98/#99, #111/#112, #114/#115, #120/#121, #122/#123): a parallel Performance Analytics session claimed #127 for its own work first; this entry, drafted independently under the same number, is renumbered to #128 rather than overwriting theirs — decisions are immutable, no entry is ever silently renumbered in place.** The core proof: a fixture-driven replay of one symbol through the real, unmodified engine pipeline (`EventBus`/`FeatureEngine`/`LevelInteractionEngine`/`MarketStateEngine`/`ContextEngine`) and a real, unmodified `Strategy.evaluate()` produces a real `BacktestRunRecord` and a real `StrategyOutcomeRecord(is_backtest=True)` resolving to it. Confirmed by direct `diff -rq` against a fresh untouched clone at every delivery: zero modification to any of the 7 real strategy files, `performance.py`, `performance_queries.py`, `models/trading_intelligence.py`, or `schemas/performance.py` — `backtest_runner/` only ever imports and calls those, never edits them. Cross-confirmed disjoint from decision #127's own work too: that entry independently states it never touched `backend/app/backtest_runner/`; this entry independently confirms it never touched `intelligence.py`, `InfoTab.tsx`, `api-client.ts`, or either Performance Analytics hook/test file.

    **Data boundary.** `FixtureCandleProvider` (`backend/app/backtest_runner/fixture_provider.py`) validates the replay/persistence plumbing only — it is explicitly not, and never claims to be, real historical-market validation; no strategy has been proven profitable by anything built here. The underlying constraint remains external: `PolygonAdapter.get_historical()` still raises on 1m/5m/15m timeframes on the current plan tier (a real `NOT_AUTHORIZED` response, confirmed again in Unit 5's own regression test against the real adapter class) — a data-provider limitation, not something this work resolves or works around.

    **Context boundary, stated precisely.** The current Context Engine does not yet provide point-in-time historical context replay — `ContextEngine._symbol_loop`/`_global_loop` are real wall-clock-timer-driven loops, and `FundamentalsProvider`/`NewsFlagProvider` call live Finnhub endpoints with no historical mode at any tier. Backtest Runner v1 does not claim this is a permanent architectural ceiling; it uses the `BacktestContextProvider` extension boundary (`context_provider.py`) instead of hardcoding "backtest context = calendar only" into the runner. `FixtureBacktestContextProvider` is v1's only real implementation — a real `MarketClock`-derived calendar provider (`symbol_providers=[]`, Fundamentals/News honestly absent, never fabricated) — and `HistoricalContextProvider` exists as a documented, deliberately-`NotImplementedError` extension point for whenever a genuinely point-in-time-capable context source exists. Neither this task nor decision #125/#126's Context Engine UI work changes any of this.

    **D17, as-built (not as originally proposed).** `market_state_at_entry`/`context_at_entry` are captured at the real `entry_filled_at` instant — the fill candle, one candle after the signal — never at the signal candle itself; `market_state_at_exit`/`context_at_exit` likewise at real `exit_filled_at`. This corrects the task's own original framing (which spoke of gating "entry" generically): `schemas/performance.py`'s own docstring states outright that these fields are "captured at `entry_filled_at`, not `setup_detected_at`," and `state_snapshot.py`'s `capture_strategy_outcome_snapshots()` — previously unused by any real caller — is exactly the function this calling convention was already built for. If either capture returns `None` at its instant, the signal is discarded (a `DiscardedSignal`, not a `StrategyOutcome`) — no fabricated `{}` is ever substituted, confirmed directly in Unit 5's regression tests via the actual Runner code path, not only `gate_and_warmup.check_entry_allowed()` in isolation.

    **Architecture dependency boundary.** Backtest Runner v1 depends on nothing beyond what decisions #120/#122 already built: it does not touch, call, or assume the existence of a Decision Engine, a Governor, live execution, or a promotion workflow — none of which exist yet (D4, Opportunity Engine ranking, stays deliberately open, untouched by this work). It produces `StrategyOutcome` rows; what happens to them is Performance Intelligence's concern, not this module's — including decision #127's own new `performance_queries.py`-exposing route, built entirely independently and read-only with respect to this work.

    **Isolation.** `is_backtest=True` is set on every row this module writes, via the same column decision #120 built and decision #122's queries already enforce as a strict either/or selector — reused, not reproven from scratch (Unit 5's own isolation test confirms a live-query pattern never sees a backtest row, using the existing invariant rather than rebuilding it).

    **Replay architecture, as-built:**
    ```
    Fixture Candle Data
            |
    ReplayStateProducer (EngineBackedReplayStateProducer)
            |
    candle_ts -> real Feature/LevelInteraction/MarketState/Context state
            |
    Strategy.evaluate() — called on EVERY replayed candle, matching live
    (acting on an Opportunity is a Decision Engine concern that doesn't
    exist yet — skipping evaluate() mid-trade would corrupt a strategy's
    own internal state, e.g. ORB's candles_seen, in a way live never does)
            |
    Fill Simulator (pure, MarketClock-derived — next-open entry,
    forward-walk exit, stop-wins same-candle tie, real-session-close
    eod_flatten; InsufficientReplayDataError kept distinct from
    eod_flatten when the fixture simply runs out before real close)
            |
    record_strategy_outcome() — real Postgres write, is_backtest=True
            |
    Performance Intelligence (decisions #120/#122/#123/#127 — unchanged)
    ```
    `EngineBackedReplayStateProducer` contains all wall-clock waiting (`MarketStateEngine`'s real ~1s debounce floor — confirmed unavoidable without redesigning that engine, out of scope) behind a `candle_ts -> state` contract; `BacktestRunner` itself has no sleep of its own. `engine_singleton_guard.py` serializes runs in-process (`asyncio.Lock`, confirmed to actually block concurrent installs, not merely produce a coincidentally-ordered log, in Unit 5's own concurrency test) — concurrent backtests in one process remain explicitly unsupported, documented rather than silently assumed safe.

    **Full-suite validation, real local Postgres 16, run against current `main` (including decision #127's own work, synced in mid-close-out once its collision was discovered) before this entry's own documentation-only changes.** 702 collected, 700 passed, 2 failed. Both failures checked against the documented decision #119 cluster individually, not assumed: `test_daily_levels_carry_level_interaction_once_touched` reproduces the cluster's own documented intermittent/order-sensitive signature exactly — passed clean in isolated rerun, consistent with its full history in this log (decisions #113 onward). `test_feature_engine.py::test_vwap_publishes_even_while_sma_is_still_warming_up` matches the cluster by name and by its long-documented general fragility (this same test's exact-equality assertion has needed updating for `session_volume`, then `vwap_ext`/`session_volume_ext`, each time logged as "expected, not a regression") — but this run's specific cause is a newly-identified, previously-unlogged instance of that same pattern: decision #111 made `_update_gap` publish `regular_open` as its own `features` key for any candle at/after real regular-session open, and this test's fixture candle (`_et(2026, 8, 11, 9, 30)`, exactly regular-session open) has published it ever since — decision #111's own text only mentions updating "three existing gap tests," not this one, so the assertion was never widened for it. Reproduces deterministically in isolation (not timing-sensitive for this specific cause, unlike the daily-levels member) — confirmed by direct rerun. Flagged here precisely rather than folded silently into "2 known #119 failures, nothing to see": this is a genuine, minor, pre-existing test-staleness gap, unrelated to and not introduced by any Backtest Runner or Performance Analytics work, and out of scope for this administrative close-out to fix (would require editing `test_feature_engine.py`, not documentation). Zero regressions from either parallel track — same 2 failures, same causes, before and after both merged.

    **Footprint of this entry's own changes:** this file, the matching `INDEX.md` row, `TESTING.md`, and a targeted addition to `strategy-engine-design.md` §7 (one diagram, one "as-built" note). Zero changes to `backend/app/` from this entry itself — confirmed by `diff -rq` against a fresh untouched clone (decision #127's own files, synced in during this close-out to resolve the numbering collision, are pre-existing upstream work, not authored by this entry). The optional `backend/app/api/routes/backtest.py` remains deferred, a separate follow-up decision, not built or tested here.

---

129. **Closes the exact `regular_open` test-staleness gap decision #128 identified and deliberately left unfixed (out of scope for that entry's own documentation-only close-out).** `test_feature_engine.py::test_vwap_publishes_even_while_sma_is_still_warming_up` widened to include `regular_open` in its exact-equality features dict. Re-checked the decision-log tail (three-source: `INDEX.md`'s last row, `confirmed-decisions.md`'s tail heading, `archive/*.md`'s own last-archived range) both before reading and immediately before writing this entry, via a full fresh re-pull diffed against this session's own working copy — #128 stayed latest both times, no collision to reconcile.

    **Root cause, confirmed directly against `_update_gap` (`app/feature_engine/engine.py`), not assumed from #128's own framing.** Decision #111 made `_update_gap` publish `regular_open` as its own `features` key whenever `state["regular_open"] is not None` — true the moment today's regular session has opened, deliberately independent of whether `gap_pct`/`gap_dollars` are also computable yet (see that method's own docstring, "published whenever known ... not gated behind `gap()`'s own `{}` result"). This test's fixture candle is published at `_et(2026, 8, 11, 9, 30)` — exactly regular-session open — so `regular_open: 100.0` has been a real, present key in this test's actual `features` payload ever since #111 landed. #111's own text names only "three existing gap tests" as updated for the new key; this test lives in the VWAP-warmup test group, not the gap test group, so it was never touched and its hardcoded exact-equality dict silently fell out of sync with the real payload — the same "hardcoded dict needs widening every time a new key joins the set" shape this test's own comment already documents happening twice before (`session_volume`, then `vwap_ext`/`session_volume_ext`), just never caught for this specific key until #128's targeted trace.

    **Confirmed genuinely deterministic for this cause, and confirmed genuinely intermittent for the #119 cluster's other current member — the two were not conflated.** `test_vwap_publishes_even_while_sma_is_still_warming_up` failed identically on every isolated rerun (4/4) both before this fix (same missing-key diff every time) and would with high confidence continue to on `main` indefinitely, since nothing about `_update_gap`'s trigger condition depends on timing or ordering — a real, permanent staleness, not noise. By contrast, `test_intelligence_routes.py::test_daily_levels_carry_level_interaction_once_touched` (the #119 cluster's other currently-failing member) was re-run in isolation 5 times during this task's own verification: failed once, passed clean the other four — consistent with its long-documented order/timing-sensitive signature (decisions #113 onward) and left completely untouched, per this project's standing rule that the #119 cluster is a known baseline fixture, not something to investigate.

    **Fix, scoped to exactly the one assertion.** Added `"regular_open": 100.0` to the test's expected dict, with an inline comment naming both #111 (why the key exists) and this entry (why it's now asserted) — matching this test file's own established convention of explaining *why* the exact-equality shape includes what it includes, not just what it includes. No change to `_update_gap`, `gap.py`, or any other test in this file. `backend/app/` is completely untouched by this entry — confirmed by `diff -rq` against a freshly re-pulled clone immediately before writing this entry: the only file this change touches anywhere in the repo is `backend/tests/test_feature_engine.py`, plus this entry, its matching `INDEX.md` row, and `TESTING.md`.

    **Verification.** Real local Postgres 16 (provisioned directly in this session — `apt-get install postgresql`, `service postgresql start`, `trading`/`trading`/`trading_workspace`, `alembic upgrade head`). Before this fix, on a freshly-pulled untouched clone: `test_vwap_publishes_even_while_sma_is_still_warming_up` run in isolation 3x, failed identically all 3 times with the exact predicted diff (`regular_open: 100.0` present but unasserted); full suite 702 collected, 700 passed, 2 failed — matching decision #128's own reported baseline exactly. After this fix: the target test passes, 3/3 isolated reruns; full suite re-run twice against the same live database, both times 702 collected, 701 passed, 1 failed (`test_daily_levels_carry_level_interaction_once_touched`, the #119 cluster's own documented flake, separately confirmed intermittent above) — exactly the one failure this entry set out to close, zero regressions, zero new failures.

---

130. **Backtest Runner v1's deferred "optional API route" (decision #128) built — `POST /backtest/run`, the first real, directly HTTP-callable entry point to `BacktestRunner`.** Before this, the only working example of constructing `BacktestRunner` anywhere in the codebase was `test_backtest_runner.py`'s own fixture-construction helpers — triggering a real backtest meant writing Python against internal constructor args. Re-checked the decision-log tail (`INDEX.md`'s last row, `confirmed-decisions.md`'s own tail, `archive/`'s last-archived range) both before starting and immediately before writing this entry — #129 stayed latest both times, no collision to reconcile. Two design calls made directly by Saqib mid-task, both bigger than this task's own original framing anticipated: (1) fixture scenarios should be engineered per-strategy to actually clear each real strategy's MATCH condition wherever achievable, not one generic session; (2) `scheduler.py`'s private `_default_registry()` should be promoted to a real public `default_registry()` rather than reached into privately.

    **A real, previously-undocumented structural finding governed most of this task's scope — found by running code, not by reading it.** Attempting to build a guaranteed-fire scenario for ORB first (steep synthetic uptrend, clean opening-range breakout) surfaced that `MarketState.volume_regime_score` stayed `0.00` for the entire replay regardless of candle shape. Traced to source, then confirmed by direct execution rather than assumed from the trace: `FeatureEngine`'s `rvol`/`atr_14_pct` (backing `volume_regime_score`/`volatility_regime_score`) are populated exclusively from `self._daily_candle_cache`, itself populated only by `_maybe_refresh_daily_levels()` calling `broker_registry.get_historical_provider().get_historical(symbol, "1d", ...)`. `EngineBackedReplayStateProducer` constructs a brand-new `FeatureEngine` with no historical provider wired in at all, and `BacktestRunner` never touches `broker_registry` — so both scores are structurally `0.0` for **any** BacktestRunner replay today, for any symbol, regardless of how the fixture candles are built. Checked against every one of the 7 real strategies' actual MATCH-stage code (not docstrings): **ORB, Gap, Volume Spike, and Momentum all hard-gate MATCH on `volume_regime_score >= 45.0`** — structurally unreachable, full stop. **First Pullback, Reversal, and VWAP gate MATCH only on `trend_score` and a real `LevelInteractionEngine` touch/resolution**, neither of which touches the daily-candle cache — genuinely reachable. This was surfaced to Saqib directly with three options (guaranteed-fire for the 3 reachable strategies + honest best-effort for the other 4 and log the gap; build a temporary fixture daily-history seam into `broker_registry` so all 7 can fire; fall back to generic scenarios) before proceeding — the first was chosen. New open item **D18** (`strategy-engine-design.md` §10) records this precisely, including why the "bigger" fix (a fixture historical-data seam) is real, separate, and non-trivial: `broker_registry` is a process-wide singleton also depended on by live trading, so a fix has to not risk clobbering a real connected provider mid-session, not just satisfy a backtest run in isolation.

    **Scenarios, each verified by direct execution against the real `BacktestRunner` + real strategy, not reasoned about from reading strategy source — and re-verified a second time from their actual checked-in CSV form, not just the in-memory candle objects used to build them** (`backend/app/backtest_runner/fixtures/*.csv`, loaded via the already-built-but-previously-unused `fixture_provider.load_fixture_candles_csv()` — closes a small existing gap rather than inventing new machinery):
    - `first_pullback_vwap_dip` (130 candles) — established uptrend (steep enough to clear `trend_score >= 60` with margin) + a single engineered dip into VWAP's aura band that bounces back out the same side (REJECTED, `entered_from="above"`), genuinely the day's first touch (candle 0 deliberately given a wide range so its own typical-price-vs-close gap exceeds the 0.2% aura band, avoiding a spurious cold-start touch that would have made the designed dip touch #2, not #1). Real persisted row confirmed: `FirstPullback, BUY, entry=106.585, exit=107.857, exit_reason=target, realized_r=2.0`.
    - `reversal_vwap_break` (140 candles) — same uptrend base, but the dip breaks all the way through VWAP (CONQUERED, opposite side from `entered_from`) instead of bouncing; the established uptrend then resumes (the reversal attempt fails), cleanly stopping the resulting SELL out — a real, honestly-recorded losing trade, not a fabricated winner. Confirmed: `Reversal, SELL, entry=105.314, exit=105.949, exit_reason=stop, realized_r=-1.0`.
    - `vwap_neutral_conquest` (140 candles) — flat/choppy session (near-zero net drift, `trend_score` held at ~50 throughout, confirmed directly against `scoring_utils.trend_established_side`'s exact neutral band, `40 < trend_score < 60`) with a genuine VWAP conquest partway through, plus a decisive follow-through decline afterward so the resulting SELL actually resolves within the fixture instead of hitting `InsufficientReplayDataError` (hit on the first attempt without the follow-through — caught by running it, not by predicting it). Confirmed: `VWAP, SELL, entry=99.978, exit=99.087, exit_reason=target, realized_r=2.0`.
    - `volume_gated_baseline` (120 candles) — generic moderate-uptrend session, deliberately NOT shaped to attempt triggering any particular strategy. Chosen over four separate "shaped-but-futile" scenarios (a breakout shape for ORB, a gap shape for Gap, etc.) on the reasoning that building candle data engineered to look like an attempt at a MATCH condition that is structurally unreachable regardless of shape would misrepresent the situation, not honestly represent it — one plainly-labeled fallback is more honest than four fake-precision ones. Verified clean (no crash, no unexpected discard) against all four volume-gated strategies individually: `outcomes_recorded=0, discarded_signals=[]` for ORB, Gap, Volume Spike, and Momentum alike.

    Any `(strategy_name, scenario)` pair is accepted by the route — `scenarios.py` does not enforce "only run FirstPullback against `first_pullback_vwap_dip`." The per-strategy names describe what each scenario was built to demonstrate, not a restriction; running a mismatched pair honestly returns `outcomes_recorded=0`, same as any of the four volume-gated strategies against anything.

    **`_default_registry()` promoted to public `default_registry()`** (`app/strategy_engine/scheduler.py`) — Saqib's direct call over reaching into the private name. Behavior completely unchanged; both call sites (`StrategyScheduler.__init__`'s own default-registry fallback, and the module docstring's D14 note) and `test_strategy_scheduler.py`'s import/usages updated (4 call sites), a docstring note added explaining the promotion and pointing at this entry. `test_strategy_scheduler.py`'s own 30 tests re-run clean immediately after the rename, before building anything on top of it.

    **The route itself** (`backend/app/api/routes/backtest.py`, registered in `main.py` alongside the other routers) — `POST /backtest/run`, query params `strategy_name`/`symbol`/`scenario` (all three required, no silent defaults, matching this codebase's existing POST convention of plain query params rather than a Pydantic body — `broker.py`'s `/subscribe` is the precedent, confirmed directly rather than assumed). `strategy_name` validated against `default_registry()`'s real `.name` values; `scenario` validated against `scenarios.py`'s registry; both reject with a 400 listing the valid set, matching `intelligence.py`'s own `/series` 400-shape convention (read for convention only, not modified). `symbol` normalized `.strip().upper()`, same convention `scanner.py` already uses. Response is `dataclasses.asdict(result)` of the real `BacktestRunResult` — `run_id`/`sweep_id`/`outcomes_recorded`/`discarded_signals` verbatim (all four real fields, not only the three this task's own prompt named as illustrative) — confirmed FastAPI's default `jsonable_encoder` correctly serializes the resulting UUID/datetime values with no extra handling needed, checked directly against a throwaway endpoint before relying on it. No Performance Analytics wrapping: a caller wanting the raw persisted rows can already query the existing, unmodified `/intelligence/strategy-outcomes` separately, per this task's own scope boundary. Strategy is looked up via a **fresh** `default_registry()` call per request (fresh strategy instances, no cross-request state pollution — matches every existing test's own construction pattern). `data_version` is `f"fixture:{scenario}"` (embeds which scenario honestly, rather than an opaque constant); `feature_version` is a route-level constant (`"feature_engine_v1"`, mirroring `test_backtest_runner.py`'s own value, the only prior place in the codebase that had to pick one) — `BacktestRunner` itself has no default for either, deliberately, per its own module docstring, and this route is now that caller. None of `intelligence.py`, `performance_queries.py`, the 3 frontend files, any of the 7 real strategy files, `performance.py`, `models/trading_intelligence.py`, or `schemas/performance.py` were touched — confirmed by `diff -rq`, not merely by intent, below.

    **Replay latency treated as a real API-design constraint, not a footnote — raised directly by Saqib mid-task after the first draft only documented it.** `EngineBackedReplayStateProducer` costs a measured, real ~1 second of engine-settle time per replayed candle (confirmed by direct per-candle timing, not estimated) — so this route's own response time is genuinely proportional to scenario length, ~120-140 seconds for this task's own scenarios. Checked rather than assumed whether that's actually safe to ship as a synchronous call in this specific deployment: this repo's `Dockerfile`/`docker-compose.yml` run a single uvicorn worker with no reverse proxy and no `--timeout-keep-alive` override, and `TestClient`'s own in-process ASGI transport was directly confirmed NOT to enforce httpx's normal 5-second default client timeout (a throwaway `asyncio.sleep(7)` endpoint returns successfully through it) — so nothing in this specific stack today would truncate a multi-minute synchronous request, though a different deployment (a proxy or load balancer added later) could behave differently, which this route has no way to know or control, and says so. Explicitly did NOT redesign this into a background job, polling, or webhook infrastructure — no such pattern exists anywhere else in this codebase, and this task's own scope is "a route + strategy lookup," not new async infrastructure; the route's own docstring states this is a deliberate v1 trade-off, not an oversight. Also restated plainly, not left findable only by reading `engine_singleton_guard.py`: this route is the first thing that makes that module's already-documented "unsafe to run concurrently with live trading" singleton-swap risk directly, easily HTTP-reachable rather than requiring someone to write Python against internal constructor args — the risk itself is not new, but how easily it's now triggered is.

    **Tests** (`backend/tests/test_backtest_routes.py`, new, real Postgres, DB-gated) — deliberately does not re-test `BacktestRunner`'s own orchestration (`test_backtest_runner.py`'s job); tests this route specifically. Two tests are genuinely slow by design, not by oversight: `test_run_backtest_first_pullback_scenario_fires_and_persists` (~130s) and `test_run_backtest_volume_gated_strategy_returns_honest_zero` (~120s) each measure their own wall-clock elapsed time and the first asserts a generous floor (`elapsed > 60`) specifically so a future change that accidentally short-circuits the replay would fail this test rather than silently ship a faster-but-wrong response — a deliberate response to the same "don't let a passing test hide a real timeout/problem" concern above, since `TestClient` would pass either way. Both also verify against the DB directly (real `strategy_outcomes` row, matching `test_backtest_runner.py`'s own posture of not just trusting the response body). Three fast tests (sub-second, no replay triggered): unknown `strategy_name` → 400 listing valid names; unknown `scenario` → 400 listing valid scenarios; missing required query param → FastAPI's own 422, confirming no silent default was introduced anywhere. 5 tests, run both individually and together, 100% stable; combined runtime ~248s, a real and deliberate cost this module's own docstring explains rather than hides.

    **Docs.** `strategy-engine-design.md` §7 extended (not duplicated) with a small new diagram (`POST /backtest/run` → strategy lookup → scenario lookup → `BacktestRunner.run()` → response) directly beneath the existing as-built (#128) diagram, plus the two findings above (structural volume-gate ceiling, replay latency) stated in prose immediately after it — a full new diagram for the internal replay pipeline itself was judged unnecessary since the existing #128 diagram already covers it correctly and completely. New open item D18 added to §10, exact text above.

    **Footprint, confirmed by `diff -rq` against a freshly re-pulled clone of `main`** (re-pulled after this task began, confirming no parallel session landed anything new at the same time — still #129 latest both times checked): `backend/app/api/routes/backtest.py` (new), `backend/app/backtest_runner/scenarios.py` (new), `backend/app/backtest_runner/fixtures/*.csv` (new, 4 files), `backend/app/main.py` (2-line router registration), `backend/app/strategy_engine/scheduler.py` (the promotion rename above, docstring only otherwise), `backend/tests/test_strategy_scheduler.py` (import/usage updates for the rename), `backend/tests/test_backtest_routes.py` (new), `docs/architecture/strategy-engine-design.md` (§7 extension + D18), this entry, the matching `INDEX.md` row, and `TESTING.md` — no accidental touch to any of this task's explicit boundary files.

    **Full-suite verification, real local Postgres 16 (provisioned directly in this session — `apt-get install postgresql`, `alembic upgrade head`, fresh wipe-and-recreate before both the baseline and the final run).** Baseline, freshly re-pulled untouched clone: 702 collected, 701 passed, 1 failed (`test_daily_levels_carry_level_interaction_once_touched` — the #119 cluster's own documented intermittent flake, matching decision #129's own reported post-fix baseline exactly). Working copy, all of this task's changes included: 707 collected, 706 passed, 1 failed — the identical single failure, exactly +5 collected/passed (this task's own new test file), zero regressions. Confirmed both with the new test file run in isolation per-test and as one combined file run, same results both ways.

---
