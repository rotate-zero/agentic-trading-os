# Confirmed Decisions

**Extracted from:** `../architecture/system-design.md` §9 (original numbering preserved for continuity with any existing references).
**Companion documents:** [`../architecture/system-design.md`](../architecture/system-design.md) (how it's built), [`../architecture/trading-intelligence-architecture.md`](../architecture/trading-intelligence-architecture.md) (how it thinks), [`future-ideas.md`](./future-ideas.md) (deferred, not rejected), [`../roadmap/phase-roadmap.md`](../roadmap/phase-roadmap.md) (when things ship).

This is the running log of settled architecture decisions — the "why did we choose X" record, kept in one place instead of scattered as inline asides across growing documents. **New confirmed decisions get appended here, numbered sequentially, as part of the same change that makes them** — not batched up and written after the fact.

---

**A note on this file's structure (updated at decision #106's rollover):** this file holds only the MOST RECENT range of decisions — currently #107 onward. Decisions #1–#60 live in `archive/001-060.md`; decisions #61–#79 live in `archive/061-079.md`; decisions #80–#90 live in `archive/080-090.md`; decisions #91–#106 live in `archive/091-106.md`. All four moved verbatim and unedited at the point each rollover happened. See `README.md` in this directory for the full read/write/maintain protocol — every Claude session touching this project should read that file before writing a new decision here, not just this note.

---

107. **First Pullback and Reversal — design note locked (Stage 0, no code) — the last two strategies from trading-intelligence-architecture.md §8's planned v1 set (ORB, Gap, Volume Spike built; Momentum still assigned elsewhere).** Full walkthrough, diagrams, and open items: `strategy-engine-design.md` §16. Both read `LevelInteractionEngine`'s touch/holding/rejected/conquered vocabulary (decision #46) as a settled input, the same "consume, don't rebuild" boundary discipline `orb_strategy.py` already applies to Market State's `trend_score`/`volume_regime_score` — neither strategy re-derives zone classification, Aura width, or touch counting itself.

    **First Pullback — is this the trend's first pullback to a key reference level today, and did the level hold?** GATE requires an established trend (`trend_score` past `trend_score_threshold`, same mirror-around-50 convention and `>50` guard as `match_direction()` in `orb_strategy.py`/decision #99) and `touch_count_today == 1` for the configured `level_key` (a `StrategyConfig.params` value, v1 default `"vwap"` — same "not its own strategy class" precedent §3 already sets for SMA 9/20). MATCH confirms the touch resolved back out the side it entered from (REJECTED, `LevelInteractionEngine`'s own definition) in the trend's favor; a touch that resolves CONQUERED (opposite side) is a non-match, not an error. Fires at most once per symbol per day.

    **Reversal — has an established trend's key level just been conquered against it?** Same touch-tracking mechanism, opposite confirming outcome: MATCH fires when a touch resolves CONQUERED while `trend_score` still shows the OLD, about-to-be-broken direction — deliberately not gated to the first touch only (`touch_count_today` read for SCORE, never gates MATCH), since a reversal is often the second or third test that finally breaks, not the first. Direction is the mirror of the trend being broken (an established uptrend conquered downward proposes SELL).

    **Real gap found and resolved without changing `base_strategy.py` (§10's new D9).** Neither strategy can read `LevelInteractionEngine`'s own `status` field (`"rejected"`/`"conquered"`) directly — that field only exists on the transient `LevelInteractionChanged` event; `get_snapshot()` (decision #47) exposes only the current steady `zone`, already moved on by the time a resolution is visible. Subscribing to `LevelInteractionChanged` directly would need `on_event(...)` triggers wired to a live Scheduler that doesn't exist — `base_strategy.py`'s own docstring already flags that Scheduler as unbuilt, downstream integration work no v1 strategy (ORB/Gap/Volume Spike, all `every_candle`) has needed yet. Rather than build that wiring for two strategies ahead of need, both call `get_level_interaction_engine().get_snapshot(symbol)` directly inside `evaluate()` — the same free-function-singleton pattern `orb_strategy.py` already uses for `get_market_clock()` — and re-derive rejected-vs-conquered themselves, one candle late, from their own private per-symbol state remembering `entered_from` while `zone == "inside_aura"`, then comparing it to whatever zone the level resolves to on a later candle. Deliberately does NOT read `get_snapshot()`'s `seconds_in_zone` field: computed off wall-clock `datetime.now()` (`level_interaction_engine.py`'s own `get_snapshot()`, built for the UI panel, decision #47), not `features.candle_ts` — reading it inside `evaluate()` would violate §7's backtest-safety invariant. Neither strategy's MATCH logic needs it.

    **`allows_waiting` stays `False` for both, v1.** First Pullback's "touch just started, not yet resolved" moment is a natural fit for a `status="waiting"` Opportunity (§8's ACT/WAIT/ABANDON model) rather than silently returning `None` until resolution — but D5 (the waiting-value model itself) is explicitly deferred until a real strategy needs it, and no strategy has triggered that yet. Flagged rather than built speculatively: First Pullback would be the first real trigger for D5, not decided here.

    **Still Stage 0 — no application code.** `first_pullback_strategy.py`/`reversal_strategy.py` and their tests are the next session's work on this thread.

---

108. **First Pullback/Reversal design review incorporated, and `LevelInteractionEngine.get_snapshot()` gains a `last_applied_candle_ts` field — both before any strategy code was written.** Full response to each review point: `strategy-engine-design.md` §16, "What the design review changed."

    **Two real gaps found by inspecting the actual engine implementation, not assumed equivalent.** The review asked for the touch-resolution reconstruction (decision #107) to be checked against `LevelInteractionEngine`'s own logic rather than trusted on the original sketch's word — doing so found it wasn't equivalent as drafted. **Gap-through** (price jumps straight between `below`/`above`, `inside_aura` never observed — the engine still counts it as a touch and always calls it `conquered`) was invisible to the original design, which only started watching on an `inside_aura` observation. **Cold-start-unknown-origin** (`entered_from is None`, the engine's own "can't determine which side," decision #46) would have been silently guessed at rather than left unclassified. Both fixed in a new shared module, `level_touch_tracking.py` — see its own module docstring for the exact mechanism — used by both strategies rather than duplicated, since two independent copies of "what counts as rejected vs. conquered" was the exact risk the review was checking for.

    **A third, more serious issue, not raised by the review, found by reading `_worker_loop`/`_process_one` directly: `get_snapshot()` is fed by an async queue + `asyncio.to_thread` worker, not computed synchronously on the triggering `FeaturesUpdated`.** No built-in guarantee a strategy's read reflects the candle it's currently evaluating, and — unlike Context Engine's own `get_snapshot()` (`evaluated_at`) — no recency field existed to detect this. Resolved with a small additive fix: `get_snapshot()` now exposes `last_applied_candle_ts` per entry, sourced from `_last_applied_ts`, a value the engine already tracked internally for its own out-of-order guard, just not previously surfaced. Backward-compatible (additive field only); full existing suite re-run clean against this change before any strategy code touched it. Both strategies check this field before calling into `level_touch_tracking.py` at all, skipping the candle entirely on a stale read rather than feeding it through the tracker (which would corrupt the tracker's own zone history — see that module's docstring).

    **MATCH/SCORE boundary and invalidation framing tightened per the review, confirmed elsewhere.** `volume_regime_score`/resolution-distance/touch-count were already SCORE-only in the original sketch (flagged in both strategies' docstrings as a deliberate divergence from `orb_strategy.py`'s own MATCH-stage volume floor — not retrofitted onto ORB/Gap/Volume Spike, an open question for later). `structural_invalidation = anchor_price` unchanged, but both docstrings now state explicitly it's a thesis boundary ("falsified if a later candle closes back through this value"), not an executable stop price — that translation is Trade Planning Engine/Position Monitor's job, neither built yet. `base_strategy.py` untouched throughout, per the review's own point 3 — the `get_level_interaction_engine().get_snapshot()` polling pattern (D9) stands as designed.

---

109. **First Pullback built — `first_pullback_strategy.py`, against the reviewed design (decisions #107/#108).** MATCH: established trend (`trend_score` past `trend_score_threshold`, mirror-around-50, `>50` guard — same shape as `orb_strategy.py`'s `match_direction()`) + `touch_count_today == 1` for `level_key` (params default `"vwap"`) + that touch resolved REJECTED, with `entered_from` agreeing with `trend_score`'s direction (a mismatch is `None`, never forced). SCORE: trend strength + `volume_regime_score` + the resolving candle's own `distance_pct`, blended 0.40/0.30/0.30. Invalidation: `anchor_price`, captured at touch start via `level_touch_tracking.py`, never re-derived. Private per-symbol `_FirstPullbackState` (mirrors `_ORBState`'s shape) tracks `fired`/`trading_day` plus one `LevelTouchState`. `allows_waiting` stays `False` for v1 — flagged as D5's natural first trigger, not built speculatively.

    **Verification:** `backend/tests/test_first_pullback_strategy.py` (16 tests) — pure GATE/MATCH/SCORE math, the staleness guard in isolation (stubbed engine, no DB), and end-to-end `evaluate()` against a REAL `EventBus`/`LevelInteractionEngine`/Postgres (first-touch-rejected fires BUY; conquered doesn't fire; a second touch resolving doesn't fire even though otherwise matching; no established trend doesn't fire; day rollover permits a fresh first touch). Full suite: 486 passed, 0 failed.

---

110. **Reversal built — `reversal_strategy.py`, against the reviewed design (decisions #107/#108).** MATCH: established trend (same threshold shape as First Pullback) + the level resolved CONQUERED for `level_key` — deliberately NOT gated to `touch_count_today == 1` (a reversal is often the second or third test that finally breaks, per the design review's own point 6). Direction is the mirror of the trend being broken. SCORE: trend strength + `volume_regime_score` + `touch_count_today` (capped at 4), blended 0.40/0.30/0.30 — rewards a level that held multiple times before finally breaking. Invalidation: `anchor_price` when available; falls back to `features.features.get(level_key)` (the level's live current value) for the gap-through case, which never has a captured anchor — flagged explicitly in the module docstring as a real, if rarer, precision trade-off, not a silent degradation. Fires at most once per symbol per day (`_ReversalState.fired`, a plain `bool` — module docstring explains why ORB's `fired_directions` set precedent doesn't apply here).

    **Verification:** `backend/tests/test_reversal_strategy.py` (12 tests) — pure GATE/MATCH/SCORE math, the staleness guard in isolation, and end-to-end `evaluate()` against real infrastructure (established uptrend conquered fires SELL; a rejected touch doesn't fire; a SECOND touch conquering still fires, unlike First Pullback; a gap-through correctly falls back to the live level value; fires at most once per day even when a later touch the same day would otherwise re-match). Plus `backend/tests/test_level_touch_tracking.py` (13 tests, pure) covering `level_touch_tracking.py` itself directly — every scenario in that module's own docstring. Full suite: 486 passed, 0 failed (445 pre-change baseline + 41 new tests, zero regressions).

---

111. **Gap/Volume Spike design review's agreed changes made — six items, spanning `base_strategy.py`, `feature_engine/engine.py`, `gap_strategy.py`, `volume_spike_strategy.py`, and (discovered mid-change) `orb_strategy.py`/`first_pullback_strategy.py`/`reversal_strategy.py`.** Follows a structured design-level review of decisions #104/#105 (full review preserved in this session's own record, not duplicated here — this entry covers what changed and why, not the review's full reasoning). Six items, all six made:

    **1 — `regular_open` published by Feature Engine, no longer reconstructed.** The review's §1 questioned `gap_strategy.py`'s own `regular_open = pdc + gap_dollars` algebra: a generic market fact (same category as `pdc`/`pdh`/`pdl`, already published), reconstructed only because Feature Engine tracked it internally (`_update_gap`'s own state) without exposing it — an ownership smell, not a hard boundary violation. `_update_gap` now publishes `regular_open` as its own `features` key, independent of `gap_pct`/`gap_dollars` (known the moment today's regular session opens, even without a prior day to compare against). `gap_strategy.py` reads it directly; the reconstruction is gone. Test coverage added to `test_feature_engine.py`'s three existing gap tests (one runs DB-free in this sandbox and passes; two are DB-gated, same standing limitation as every DB-gated file in this log).

    **2 — Gap bounded to a maximum age since the open (`max_minutes_since_open`, v1 default 60, unvalidated).** The review's §2 found nothing previously stopped a match hours after the open — a 2pm reclaim of `regular_open` read identically to a clean 9:30 hold. "Gap and go" is conventionally early-session; unbounded, Gap would only grow more redundant with Momentum/VWAP once those exist. Checked in `match_direction()` via a plain `minutes_since_open` int computed by `evaluate()` (`MarketClock.minutes_since_open()`), keeping the pure function itself clock-free — same discipline every pure function in this codebase already follows. Does NOT resolve the review's deeper, separately-flagged D-level question (whether an instantaneous `close` vs. `regular_open` comparison, with no memory of the day's own history, actually proves "holding" rather than "currently on the right side") — that stays open, deferred pending real outcome data, same as the review recommended.

    **3 — Volume Spike gained an absolute-volume floor (`min_absolute_volume`, v1 default 500 shares) and a body/displacement check (`min_body_ratio`, v1 default 0.3).** The review's §3 found two concrete MATCH-stage gaps: `volume_ratio` alone can't distinguish a genuine spike from a small order against a thin/illiquid baseline (nothing else — not `volume_regime_score`, a day-level per-symbol-normalized read — protects against this); and a huge-volume, razor-thin-body candle (open/close a cent apart) previously passed as directional on `close != open` alone, despite being closer to indecision than conviction. `min_body_ratio` is computed from the same OHLC already on the candle — `body_ratio = abs(close-open)/(high-low)`, safe by construction since `close != open` (checked first) guarantees `high > low` for any valid bar. Two new pure tests plus two new end-to-end tests (a thin-illiquid-baseline scenario and a wide-range-thin-body scenario) pin both fixes against the exact failure modes the review described. Deliberately NOT addressed, both flagged by the review as needing real outcome data: whether a high-volume directional candle actually means continuation vs. exhaustion/climax, and whether sizing the stop off the spike candle's own wick can produce an impractically wide risk/target.

    **4 — `_clamp`/threshold-guard/trend-magnitude extracted to a new shared module, `scoring_utils.py`.** The review's §9 found `_clamp()` and the `trend_score_threshold > 50.0` mirror-guard copy-pasted, near-verbatim, across `orb_strategy.py`, `gap_strategy.py`, and `volume_spike_strategy.py`. `clamp()`, `trend_magnitude()`, and `validate_mirror_threshold()` now live in `scoring_utils.py` — deliberately NOT a "strategy scoring engine": each strategy's own weights and strategy-specific third SCORE component stay in its own file, only the domain-free arithmetic moved. **Mid-change discovery, folded in against the then-current repo state:** a `git pull` immediately before implementing this item surfaced that `first_pullback_strategy.py`/`reversal_strategy.py` (decisions #107–#110, built concurrently, merged after decisions #104/#105 but before this session picked the review back up) had independently duplicated the identical `_clamp`/threshold-guard pattern a fourth and fifth time — direct, real-time confirmation of the review's own "this will only get worse with more strategies" reasoning. Both folded into the same extraction rather than leaving it half-finished; verified behavior-neutral (full suite identical before/after: 327 passed, 40 pre-existing DB failures, 119 skipped). New `test_scoring_utils.py` (11 tests) covers the shared module directly, independent of any one strategy's own tests.

    **5 — `Opportunity.expected_horizon_minutes: int | None = None` added to `base_strategy.py`.** The review's §5 found every strategy (ORB included) had an implicit expectation of how long its setup should take to play out — a fast volume spike and a session-long gap continuation are not the same kind of bet — previously lost the moment `evaluate()` returned, since nothing on the schema carried it. Optional, honest-absence default: a strategy with no informed opinion leaves it `None`. Deliberately NOT an expiry mechanism (that's `wait_expires_at`'s job, WAITING-path only) — descriptive metadata for Performance Intelligence / a future Decision Engine, nothing currently reads it to auto-expire anything. Populated by ORB (45 min default, backfilled here since the schema addition would otherwise immediately reintroduce the exact gap being fixed in the very artifact meant to fix it), Gap (60 min), and Volume Spike (15 min) — each a v1 guess, unvalidated. **Deliberately NOT populated** by First Pullback/Reversal — that's a value judgment specific to those strategies' own thesis timing, left to whichever thread owns them next; the field is available to them automatically (optional, defaults to `None`) without this change speaking for that thread.

    **6 — Renumbering note.** This item was going to be decision #107 in this session's own plan, drafted before pulling First Pullback/Reversal's concurrent work (decisions #107–#110). Renumbered to #111 on pull — no content changed, only the number.

    **Where it's built:** `app/strategy_engine/scoring_utils.py` (new), `app/strategy_engine/base_strategy.py` (`Opportunity.expected_horizon_minutes`), `app/feature_engine/engine.py` (`_update_gap` publishes `regular_open`), `app/strategy_engine/orb_strategy.py`, `app/strategy_engine/gap_strategy.py`, `app/strategy_engine/volume_spike_strategy.py`, `app/strategy_engine/first_pullback_strategy.py`, `app/strategy_engine/reversal_strategy.py` (the last two: `scoring_utils.py` adoption only, no behavior change). Tests: `backend/tests/test_scoring_utils.py` (new, 11 tests), `backend/tests/test_gap_strategy.py` (+3, now 18), `backend/tests/test_volume_spike_strategy.py` (+4, now 22), `backend/tests/test_base_strategy.py` (+1, now 10), `backend/tests/test_orb_strategy.py` (assertion added to an existing test, no new test), `backend/tests/test_feature_engine.py` (assertions added to three existing gap tests, no new tests).

    **Verified, deliberately DB-free where possible.** Full backend suite re-run before and after this entire change, in this session's own Postgres-free sandbox (fresh clone, this repo state includes decisions #107–#110): identical 40 pre-existing DB-connectivity failures, 119 skipped, both unchanged; 346 passed vs. the 327-passed baseline immediately before this change — exactly these 19 new tests, zero regressions anywhere, including in `first_pullback_strategy.py`/`reversal_strategy.py`'s own 28 (skipped, DB-gated) plus already-passing tests, confirming the shared-utility fold-in didn't disturb either. **Not verified:** against a real local Postgres (none available in this session) — the two DB-gated `test_feature_engine.py` assertions for `regular_open`'s freeze/backfill behavior specifically need Saqib's own local run, same standing limitation every DB-gated file in this log already carries.

---

112. **Stage 2 (Strategy Scheduler / wiring) sequencing and scope locked — Stage 0, no code — new open item D10 (`strategy-engine-design.md` §10), staged plan updated (§12). Renumbered from a collision at #111 — see below.** Prompted by a status review confirming this thread's built strategies have nothing yet calling them against live data — `base_strategy.py`'s own "NOT BUILT HERE" note on the Scheduler is still accurate. Two calls made directly by Saqib: **(1)** Stage 2 is **blocked until Momentum/VWAP land** from the parallel session — the Scheduler gets built once against all 7 strategies, not retrofitted twice. **(2)** Stage 2's scope, once unblocked, is **wiring only**: instantiate the built strategies against their `StrategyConfig`s, read each one's `ScheduleTrigger` and subscribe it to the live event bus, call `evaluate()` with real `MarketStateEngine.get_snapshot()`/`ContextEngine.get_snapshot()` (M4, decision #98) plus the triggering `FeatureSet`, publish the result as a new `OpportunityCreated` event, and add a `get_snapshot()` read-side — the same engine → snapshot → route pattern decision #47 already established. Declarative `gate_conditions` enforcement (§2b) and Opportunity Engine's cross-strategy ranking (§9) are explicitly OUT of scope for this stage — separate, later work, not bundled in. **As of this entry, Momentum/VWAP are still not confirmed merged** — checked directly against a fresh clone, not assumed; Stage 2 remains blocked.

    **Numbering collision at #111, same category decision #99 already documents once with #98.** This decision was originally drafted and queued in a prior session as docs-only content for slot #111, but never actually applied to git before Saqib separately committed different, unrelated work (the Gap/Volume Spike design-review changes, six agreed fixes plus `scoring_utils.py`) under that same number. Resolved the same way #99 resolved the #98 collision: the queued content wasn't discarded, just renumbered and re-applied here as #112. `strategy-engine-design.md` §13 gets a new point 9 recording this so a future session doesn't need to reconstruct it from this entry alone.

    **Documentation-integrity fix made in the same change:** `strategy-engine-design.md` §13 point 7 was found stale — it still said First Pullback/Reversal were "design-locked but unbuilt," true as of decision #107 but superseded by #109/#110. Corrected in place.

    **Still Stage 0 — no application code.** Stage 2 itself is the next session's work on this thread, once Momentum/VWAP are confirmed merged.

---

113. **Momentum and VWAP built — the sixth and seventh v1 strategies, closing out `trading-intelligence-architecture.md` §8's full planned set. `scoring_utils.ESTABLISHED_TREND_SCORE_THRESHOLD` extracted; `reversal_strategy.py` refactored onto it. Stage 2 (decision #112/D10) now unblocked.** Design went through an external (ChatGPT) review before any code was written — same "design note first, then code" practice as First Pullback/Reversal (decisions #107/#108) — with three fork-in-the-road questions decided directly by Saqib rather than left to the review's own preference. This entry covers what was decided and built, not the review's full back-and-forth.

    **1 — Momentum's acceleration threshold: chosen from the formula's own semantics, not from empirical distribution analysis.** `market_state_engine/scoring.py`'s `acceleration_score()` was read directly (not assumed from the field's name): it is `trend_score`'s own rate of change, no volume folded in. Saqib's explicit call — skip a live-data calibration pass for now, pick a sensible v1 number from the formula alone, tune later once Stage 2 produces real distributions. `DEFAULT_ACCELERATION_SCORE_THRESHOLD = 65.0`, flagged unvalidated same as every other v1 calibration constant in this codebase. Encoded as a real hierarchy, not just a flat AND of three conditions: acceleration is the MATCH-stage PRIMARY condition, `trend_score` a deliberately LIGHTER context confirmation (`DEFAULT_TREND_CONTEXT_THRESHOLD = 55.0`, its own distinct params key `trend_context_threshold` — NOT the same question as "established trend," see item 3 below), `volume_regime_score` a participation floor (`45.0`, same value ORB already uses) — and the same hierarchy reappears in SCORE's blend weights (acceleration 0.45, trend 0.25, volume 0.30).

    **2 — VWAP made disjoint from Reversal by construction, not left to downstream arbitration.** Two candidate designs were rejected first: relationship-persistence (risked being "Momentum with a different gating field," the review's own words) and a naive two-candle cross (would false-trigger on the exact chop `LevelInteractionEngine`'s Aura band already exists to absorb). What shipped: VWAP fires on a genuine `conquered` resolution — reused verbatim from `level_touch_tracking.observe_resolution()`, zero new touch/cross-detection code — gated to `trend_score`'s NEUTRAL band only. Direction comes from the actual resolved zone (`"above"` → BUY, `"below"` → SELL), never a mirror of `trend_score` — the structural opposite of `reversal_strategy.py`'s own `match_direction()`, which bets against whichever trend is established and never looks at which way the conquest itself moved. Saqib's explicit call, made directly rather than "co-firing is fine" (the accepted answer for Momentum/ORB, decision below): VWAP and Reversal read the same underlying event and would otherwise near-duplicate each other on almost every candle with opposite direction conventions — worth a real, structural gate.

    **3 — `ESTABLISHED_TREND_SCORE_THRESHOLD` promoted to `scoring_utils.py`, `reversal_strategy.py` refactored onto it.** Was `reversal_strategy.py`'s own private `DEFAULT_TREND_SCORE_THRESHOLD = 60.0`. Now the single authoritative constant both `reversal_strategy.py` (fires when established) and `vwap_strategy.py` (fires when NOT) read, so the two conditions partition every `trend_score` reading with no gap and no overlap by construction, rather than via two independently-hardcoded 60/40 pairs that could silently drift apart the moment either `StrategyConfig` is retuned. New `scoring_utils.trend_established_side()` holds the actual `>=`/`<=` classification (`validate_mirror_threshold()` internally); `reversal_strategy.py`'s own `match_direction()` now delegates to it — behavior unchanged, six new/adjusted tests in `test_scoring_utils.py`, all 6 pre-existing `test_reversal_strategy.py` tests re-run unchanged and still pass. **Still open, not resolved by this change** (`strategy-engine-design.md` §10, new item D11): each strategy's `StrategyConfig.params` stays independently overridable per its own versioning — this guarantees the DEFAULTS match, not that they stay matched forever if either is retuned in a later config version without touching the other.

    **Momentum vs. ORB — co-firing accepted, no arbitration added, confirmed directly by Saqib and the review.** Genuinely different questions (ORB: did a specific structural morning-range level break; Momentum: is the market already moving and gaining force, independent of any level or time-of-day) — the two can and are expected to fire on the same candle; nothing in either file or `base_strategy.py` arbitrates between them, matching the review's own explicit recommendation against adding an "if ORB fires, Momentum can't" rule.

    **Momentum's invalidation: a private rolling swing lookback (`DEFAULT_LOOKBACK_BARS = 10`), not ATR, not `LevelInteractionEngine`.** The review preferred "the structure supporting the move failed" over "price moved X against me" — genuinely different claims, not a style preference. `FeatureSet` publishes no swing-high/low, so this needed new private per-symbol state, same category as ORB's opening range / Volume Spike's rolling baseline. Deliberately NOT the review's own suggested ATR-during-warm-up fallback: this file simply doesn't fire during the first `lookback_bars` minutes of each trading day per symbol, honest-absence, same precedent `volume_spike_strategy.py`'s own baseline warm-up already set — one fewer code path, and the warm-up window is small enough that "don't fire yet" is a fully acceptable v1 answer.

    **`structural_target` — schema-required placeholder, not a strong claim, for both.** The review's recommendation was "don't force a strategy-specific projection." `Opportunity.structural_target: float` is currently REQUIRED (not `Optional`) on `base_strategy.py` — changing that schema would be a bigger, cross-cutting change touching all 5 other built strategies, out of scope here. Reconciled by using the same mechanical R-multiple (`close ± target_r_multiple * risk`, default 2.0) every other v1 strategy already computes — a schema-completeness value, not new invented target logic.

    **VWAP's SCORE uses `distance_pct`, not `touch_count_today`.** `get_snapshot()`'s `distance_pct` field was checked directly against `level_interaction_engine.py`'s source — computed from `_latest_close`/`_latest_level_value` (candle-derived, not `seconds_in_zone`'s wall-clock `datetime.now()`), so it's backtest-safe to read. `touch_count_today` is logged in `evidence.conditions` for future calibration but NOT weighted: unlike Reversal (more prior touches before a break is straightforwardly stronger evidence), the sign of that relationship for a VWAP conquest isn't obvious — could mean a well-tested level finally giving way, or an already-choppy, low-conviction session — and guessing at a direction wasn't worth doing before real outcome data exists to check it against.

    **VWAP's cadence: fires on a genuine control TRANSITION, via `_VWAPState.last_fired_zone`, not every conquered event.** Checked directly against `level_touch_tracking.py`'s own classification rule (`"rejected" if current_zone == entered_from else "conquered"`): two CONSECUTIVE conquered resolutions in the engine's own unbroken stream always alternate zones by construction, so raw same-zone-twice-in-a-row cannot happen there. The real, reachable repeat case is more specific: VWAP is a PARTIAL observer of that stream (it skips every conquest that happens while trend is established, per item 2 above), so the zone can drift back to a value VWAP already fired for while VWAP wasn't watching. `last_fired_zone` catches exactly that case; proven both ways in `test_vwap_strategy.py` — a same-zone repeat after an established-trend window is suppressed, and a genuinely new zone still fires immediately afterward.

    **Where it's built:** `app/strategy_engine/momentum_strategy.py` (new), `app/strategy_engine/vwap_strategy.py` (new), `app/strategy_engine/scoring_utils.py` (`ESTABLISHED_TREND_SCORE_THRESHOLD`, `trend_established_side()`), `app/strategy_engine/reversal_strategy.py` (refactored onto the shared constant/helper, behavior unchanged), `app/strategy_engine/orb_strategy.py` (one stale cross-reference comment corrected — it cited the discarded draft's momentum threshold value, which this rebuild deliberately doesn't reuse). Tests: `backend/tests/test_momentum_strategy.py` (new, 18 tests, no DB dependency), `backend/tests/test_vwap_strategy.py` (new, 19 tests, DB-gated), `backend/tests/test_scoring_utils.py` (+6, now 17).

    **Verified against a real local Postgres, not just DB-free.** Unlike most entries in this log, this session provisioned PostgreSQL 16 directly (`apt-get install postgresql`, matching `app/core/config.py`'s default `trading`/`trading`@localhost/`trading_workspace`) and ran `alembic upgrade head` — every DB-gated test in this delivery, including the two multi-branch end-to-end VWAP scenarios (`test_end_to_end_same_zone_repeat_after_established_window_is_suppressed`, `test_end_to_end_new_trading_day_resets_last_fired_zone`) that would otherwise only be trace-verified by hand, actually ran and passed against a real `EventBus` + real `LevelInteractionEngine`. Full backend suite re-run before and after: 546 passed, 2 pre-existing failures (`test_feature_engine.py::test_vwap_publishes_even_while_sma_is_still_warming_up`, `test_intelligence_routes.py::test_daily_levels_carry_level_interaction_once_touched`) confirmed identical against a completely untouched second clone of `main` — pre-existing on `main`, unrelated to this change, not introduced by it, not investigated further here (out of scope for this delivery). `test_reversal_strategy.py`'s full 11 tests re-run unchanged and pass, confirming the shared-constant refactor is behavior-neutral.

    **Stage 2 (decision #112/D10) is now unblocked** — all 7 v1 strategies from `trading-intelligence-architecture.md` §8 are confirmed built and merged.

---

114. **Stage 2, Track A built — `app/strategy_engine/scheduler.py` (`StrategyScheduler`), new open item D12 (§10), staged plan updated (§12).** The first code in this repo that actually calls `Strategy.evaluate()` against live data — `base_strategy.py`'s own "NOT BUILT HERE" note on the Scheduler is what this resolves. Scope exactly as D10 locked it: instantiates all 7 built strategies via their own `default_config()`, groups by `ScheduleTrigger` (all 7 currently `every_candle("1m")`; `after_time`/`on_event` accepted into the registry but logged as unreachable — no v1 strategy uses either), calls `evaluate()` with real `MarketState`/`FeatureSet`/`ContextChanged`, publishes `OpportunityCreated`. `gate_conditions` enforcement and `active_from`/`active_to` enforcement confirmed OUT of scope directly by Saqib (same treatment as D10 already gave `gate_conditions` alone) — every registered strategy is called regardless of either. `MarketState`/`ContextChanged` reconstruction from `get_snapshot()` dicts, not new typed accessors on `MarketStateEngine`/`ContextEngine` — Saqib's call, keeps both engines' public surface exactly what M4 (decision #98) already established.

    **D12: the live trigger is `MarketStateChanged`, not `FeaturesUpdated` — found by testing, not planned.** Full reasoning in D12 (§10) and `scheduler.py`'s module docstring. Short version: `MarketStateEngine` computes asynchronously in a separate debounced worker task, not inline in its own `FeaturesUpdated` handler, so a Scheduler reacting to the same raw event has no ordering guarantee the cache is populated — proven wrong by a real-engine integration test before this shipped, not caught by inspection. Fixed by triggering off `MarketStateChanged` instead (reading `MarketState` straight from that event's payload) and caching the real `FeaturesUpdated` payload separately for `FeatureSet` (`FeatureEngine.get_snapshot()` can't supply it — its shape predates decision #99's `open`/`high`/`low`/`volume` fields and would silently null them out).

    **Exception isolation:** each strategy's `evaluate()` call wrapped individually — `EventBus._safe_call` only protects OTHER subscribers from a Scheduler-wide crash, not one strategy's failure from blocking the other 6 in the same dispatch. Verified with a real two-strategy test where the first raises and the second still fires and publishes.

    **`main.py`/`conftest.py` wired.** Scheduler starts after `ContextEngine`, stops after the bus (subscriber, same category as `MarketStateEngine`) — flagged inline as a manual-merge point since Track B (OpportunityCreated read-side, separate parallel session) independently touches the same two files. `conftest.py` gained the `_strategy_scheduler = None` reset every other engine singleton already has (an omission that would have been a real cross-test leak, same class of bug decisions #38/#47/#93 already fixed for the others).

    **Verified against a real local Postgres**, same as decision #113 — this session provisioned PostgreSQL 16 directly and ran `alembic upgrade head`. 20 new tests: registry/trigger-grouping (pure), the `FeaturesUpdated` handler's caching-only behavior (pure), the `MarketStateChanged` handler's full control flow — skip-when-absent, cross-symbol-sentinel exclusion, exception isolation, publish-on-match (pure, fake `EventBus`/`ContextEngine`) — plus two real-engine end-to-end tests: one with a stub strategy proving the actual `MarketStateChanged`-triggered translation path against REAL `MarketStateEngine`/`ContextEngine` output (kept explicitly as a regression test for the exact failure D12 describes), and one running the real 7-strategy registry against a real but MATCH-failing candle, confirming no crash and no fabricated `Opportunity`. Full backend suite re-run: 565 passed, 3 failed. **Correction to decision #113's count:** re-running #113's own "untouched second clone" check in this session found 3 pre-existing failures, not 2 — `test_vwap_publishes_even_while_sma_is_still_warming_up` (consistent), `test_daily_levels_carry_level_interaction_once_touched` (order/timing-sensitive, #113 likely didn't hit it that run), and `test_sma_ema_slope_family_groups_under_the_owning_period_and_is_excluded_from_level_interaction` (failed consistently here, a `KeyError` on `level_interaction`, same general area as the daily-levels failure — possibly a shared root cause, not investigated further, out of scope for this delivery). Confirmed directly against a completely untouched clone before treating any of the three as related to this change. Zero regressions, 20/20 new tests passing.

    **Not covered here, flagged rather than silently skipped:** a real strategy's own MATCH conditions actually firing through this full stack end-to-end (each strategy's own test file already proves that logic correct against hand-built inputs; wiring one all the way through real candle-by-candle computation to reach that same state is real, separate effort). **Also not built here — Track B's own delivery:** the `OpportunityCreated` read-side (cache + `GET /intelligence/opportunities`) — a separate, parallel session's scope, not landed as of this entry.

---

115. **Track A/Track B numbering collision at #114 reconciled — Track B's own decision record restored, its `main.py`/`conftest.py` wiring (flagged by #114 but never finished) completed and verified by running, and D13/D14 added to `strategy-engine-design.md`.** Not new feature work — a merge-reconciliation entry, same category as the #98/#99 and #111/#112 collisions this log already documents, just discovered later and with a real functional gap attached this time, not only a numbering clash.

    **What happened.** Track B (this session) built `OpportunityCache` (`app/trading_intelligence/opportunity_cache.py`) — a passive read-side cache for `OpportunityCreated`, `GET /intelligence/opportunities`, and a `main.py` lifecycle block — independently of Track A, against the `OpportunityCreated`/`Opportunity` CONTRACT rather than Track A's implementation (which hadn't landed in this session's view at build time). Both sessions checked confirmed-decisions.md's tail before writing, per standard practice, and both correctly saw #113 as the last entry — but neither could see the OTHER session's in-flight, not-yet-pushed work, so both independently logged their own delivery as #114. When Saqib applied Track A's full delivery to git, Track B's own decision-log entries (originally #114 and #115, the latter resolving the `Opportunity`-schema-location question as "D12") were overwritten rather than merged, and the `main.py`/`conftest.py` merge Track A's own #114 explicitly flagged ("MANUAL MERGE NOTE... merge both additions by hand") was never actually completed. `opportunity_cache.py`'s own code survived the merge untouched and fully passed its own 10-test suite in isolation — but `main.py` never called `opportunity_cache.start()`, so the cache was never subscribed to the live `EventBus` at all.

    **The gap, confirmed by running, not by reading.** Ran `main.py`'s real `lifespan()` against the exact repo state as pushed, published a real `OpportunityCreated` envelope onto the real `EventBus` mid-lifespan, and read `get_opportunity_cache().get_snapshot()` back: `{"symbols": {}}` — empty, despite a real publish having just happened. This confirms the gap was a genuine, live functional bug (any real `OpportunityCreated` the now-working `StrategyScheduler` publishes would have been silently dropped by the read-side, `GET /intelligence/opportunities` always returning empty), not a hypothetical — and one the current DB-less test suite can't catch on its own, since `test_opportunity_cache.py` constructs `OpportunityCache` directly, bypassing `main.py`/the singleton entirely, and no full-app-lifespan test exists for this path (matching the established precedent that `GET /market-state`/`GET /context` also have none, decision #114/original entry).

    **Fixed: `main.py` gained the two blocks Track A's own comment said were still needed** — `opportunity_cache.start()` after `strategy_scheduler.start()` in `lifespan()`'s startup, `await opportunity_cache.stop()` after `await strategy_scheduler.stop()` in shutdown, same subscriber-stops-after-the-bus posture as every other engine here. `conftest.py` gained `opportunity_cache_module._opportunity_cache = None` in the reset fixture, same pattern Track A's own delivery already established for `_strategy_scheduler`. Re-ran the identical publish-and-read-back check against the fixed `main.py`: `get_opportunity_cache().get_snapshot()` now correctly shows the published `Opportunity`, keyed by symbol and strategy, `received_at` populated — the actual wiring works end-to-end, not just each piece in isolation.

    **D13 restored** (`strategy-engine-design.md`) — the `Opportunity`-schema-location question, originally resolved as "D12" in Track B's own dropped record, renumbered since Track A's own new D12 (`MarketStateChanged` vs `FeaturesUpdated`) now occupies that letter. Resolution unchanged from the original: `Opportunity` stays on `base_strategy.py`, imported directly — `scheduler.py`'s own delivery independently confirms this in practice, importing `Opportunity` only via `Strategy`, never from a mirrored `schemas/events/` location.

    **D14 opened, not resolved here** — a genuine conflict between what Saqib told two parallel sessions, surfaced rather than silently picked either way. Track A's #114 states `active_from`/`active_to` enforcement was confirmed OUT of scope by Saqib. Separately, in Track B's own design discussion for this same Scheduler, Saqib instructed using the event/market timestamp rather than wall-clock `now` for activation-window evaluation — which only makes sense if enforcement was expected to exist. `scheduler.py` currently does not enforce either field. Needs Saqib's direct call before anyone touches `scheduler.py` on this point.

    **Verified by running.** Full backend suite, fresh venv built for this session, in this session's own Postgres-less sandbox: identical 40 pre-existing DB-connectivity failures and 158 skipped, byte-for-byte the same test names diffed directly against the exact repo state as pushed (before this entry's fixes) — 380 passed both before and after, since none of the currently-passing tests exercise the `main.py` wiring this entry fixes (see the gap paragraph above). Zero regressions from the `main.py`/`conftest.py` change itself; the fix's own correctness verified separately via the real-lifespan publish-and-read-back check described above, run against both the unfixed and fixed states for direct before/after comparison.

    **Where it's built:** `backend/app/main.py` (two additive blocks), `backend/tests/conftest.py` (one added line, same pattern as its own `_strategy_scheduler` reset), `docs/architecture/strategy-engine-design.md` (D10 status updated, D13 added, D14 opened, §12 checklist updated), `docs/decisions/confirmed-decisions.md`/`INDEX.md` (this entry). No changes to `opportunity_cache.py`, `intelligence.py`, or `scheduler.py` — all three were already correct.

---

116. **D14 resolved canonically — `active_from`/`active_to` enforcement is OUT of scope for Stage 2. Stage 0, docs only — no code changes.** Closes the conflict decision #115 opened: `StrategyScheduler` MUST NOT compare `features.candle_ts` (or any timestamp) against `active_from`/`active_to`. Every registered strategy is called on every matching trigger regardless of its config's activation window, same treatment `gate_conditions` already got in D10 — Stage 2's job is scheduling, evaluation, and publication, not enforcing a strategy's complete temporal eligibility model.

    **The two instructions that looked contradictory are reconciled, not actually in tension.** "Out of scope for now, same as gate_conditions" (confirmed directly in Track A's own thread) is the authoritative architectural decision for what Stage 2 builds. "Use event/market timestamps, never wall-clock `now`" (given in Track B's own thread) is a constraint on HOW enforcement must be implemented **if and when** it's ever brought into a future stage's scope — it was never permission to build it now. `features.candle_ts` remaining available on every `evaluate()` call doesn't by itself imply the Scheduler must currently act on it for this purpose; a field being present and a field being enforced are different questions, and only the first is true here on purpose.

    **Durable framing, so this doesn't get reopened by accident.** A future session encountering `active_from`, `active_to`, or `candle_ts` in `scheduler.py` should read their presence as expected and intentional, not as evidence D14 was missed or left half-done — those fields exist because `StrategyConfig`/`evaluate()` need them for reasons unrelated to Stage 2's own scope (versioning metadata; the backtest-safety invariant, §7), not because Stage 2 was supposed to act on them and didn't. If a later stage does bring activation-window enforcement into scope, the correct comparison is `active_from <= ts` and (`active_to is None or ts < active_to`) against `features.candle_ts` (or `market_state`'s own timestamp) — never `datetime.now()`, per §7 — but that implementation doesn't exist yet and this entry is not authorization to add it.

    **No code follows from closing this item.** `scheduler.py` was already built consistent with "out of scope" (decision #114) — this entry is the documentation catching up to confirm that was the right call, not a change to what's running. D13 (`Opportunity` stays on `base_strategy.py`, imported directly) remains accepted, untouched by this entry.

    **Stage 2 status, both tracks, as of this entry:** fully built and verified end-to-end. `OpportunityCreated` publish → `OpportunityCache` → `GET /intelligence/opportunities` confirmed working by directly running the app's real lifespan. Full backend suite against a real local Postgres: 577 passed, 1 failed — `test_vwap_publishes_even_while_sma_is_still_warming_up`, confirmed pre-existing and unrelated to any of Stage 2's work (fails identically on an untouched clone). Decision #115's own "40 failed, 380 passed, 158 skipped" figure is confirmed to be an artifact of that session's sandbox lacking a reachable Postgres, not a code regression — reproduced independently by observing the identical failure signature appear and disappear in lockstep with Postgres availability in a separate sandbox.

---

117. **Declarative `gate_conditions` enforcement built — `app/strategy_engine/gate_conditions.py` (new), `scheduler.py` wired to call it centrally, before `evaluate()`, per §2b's own text. Closes the item D10 deferred alongside `active_from`/`active_to` (decision #112) — `active_from`/`active_to` itself remains separately deferred, canonically closed by D14/decision #116, and is NOT reopened by this entry.** Scope confirmed directly against the repo before any code was written, not assumed from the task brief: grepped every one of the 7 built strategies' own `default_config()` — all 7 declare exactly `gate_conditions={"session": "regular"}`, nothing else; grepped `schemas/events/market_state.py`/`context.py` for `vix` — no such field exists anywhere, confirming §3's own illustrative `{"vix_min": 20, "session": "regular"}` schema comment doesn't correspond to anything checkable today. v1 therefore supports exactly one key, `"session"`, with exactly one recognized value, `"regular"` (checked against `MarketClock.is_regular_session(candle_ts)`).

    **A real, currently-live gap closed for 3 of the 7 strategies, not just redundancy for the other 4 — found by reading every strategy file directly, not assumed uniform.** `gap_strategy.py`, `momentum_strategy.py`, and `volume_spike_strategy.py` each already call `MarketClock.is_regular_session()` inline in their own GATE step; `orb_strategy.py` never calls it directly but is effectively self-gating via `minutes_since_open()` returning 0 whenever the market isn't open. None of the four are touched by this change — §2b's own text keeps `evaluate()`'s internal GATE for strategy-specific preconditions, and removing a strategy's own inline check is separate, later work, explicitly out of this task's scope. But `first_pullback_strategy.py`, `reversal_strategy.py`, and `vwap_strategy.py` all declare `gate_conditions={"session": "regular"}` and — confirmed directly, not assumed — have **no session check anywhere in their own `evaluate()`**. Before this change, that declared precondition was enforced by nothing at all for those three; the new central check is their only session gate, not a second layer.

    **Timestamp source: `market_state.candle_ts`, not `features.candle_ts`.** Both are present and equal in the steady state (one candle per minute), but `market_state.candle_ts` is the exact triggering event's own timestamp, already parsed before the per-strategy loop starts, with no extra cache lookup — and stays correct even in the debounce-collapse edge case the module docstring already documents, where `_latest_features`'s cached `FeatureSet` could in principle hold a different candle than the one that produced this specific `MarketStateChanged`. `features.candle_ts` was the other real candidate (§7's backtest-safety invariant is satisfied by either); this is a real call, made once and documented here rather than left implicit in the diff.

    **Unknown/malformed `gate_conditions` — fails loudly at Scheduler *construction* time, not silently at eval time, and not merely logged-as-unreachable.** Real call, deliberately differing from decision #114's own precedent for the adjacent `ScheduleTrigger.kind` case (`after_time`/`on_event`: "accepted into the registry but logged as unreachable"). Reasoning: an unwired trigger kind is an honest, visible limitation — the strategy just doesn't run yet, and nothing about its own correctness is silently wrong. An unrecognized `gate_conditions` key (or a recognized key with an unrecognized value, e.g. `{"session": "pre_market"}` — a real `Session` member this build's checker doesn't implement) is a materially worse failure: `StrategyConfig.rationale` (§3) is the operator's own record of *why* a precondition exists, and if the key it names is never actually checked by anything, the operator has silently lost a precondition they believe is protecting them — the same "honest state over fabricated state" failure decisions #89/#91/#98 already refuse to allow elsewhere. `gate_conditions.validate_gate_conditions()` runs once per strategy in `StrategyScheduler.__init__`, before trigger-grouping, and raises `ValueError` — the Scheduler itself fails to construct, so the app never starts with a `StrategyConfig` it can't actually honor. `test_construction_succeeds_for_the_real_seven_strategy_registry` is a standing regression guard: if a future strategy build ever adds a new `gate_conditions` key without teaching `gate_conditions.py` about it, that test fails at construction time in CI, not as a live surprise.

    **Extensibility, without a structural rewrite.** `gate_conditions.py`'s `_RECOGNIZED: dict[str, set[str]]` registry plus one `if` branch in `gate_conditions_satisfied()` is the whole mechanism — a genuinely new key is added by extending the registry and adding one branch, touching neither `scheduler.py` nor any strategy file. No support built for `"vix_min"` or anything else not already real in this codebase, per the task's own explicit instruction not to pre-build for keys that don't exist yet.

    **Per-strategy isolation, matching the existing exception-isolation pattern exactly.** The gate check sits in its own `try`/`except` inside the same per-strategy loop `evaluate()`'s own try/except already lives in, immediately before it — a bug raised while checking one strategy's `gate_conditions` logs and skips only that strategy, same "one bad strategy can't block the rest" guarantee decision #114 already established for `evaluate()` itself. A failed gate check logs at `logger.debug` ("honest gate skip, not an error" — the same framing/level already used for the existing features/context-absent skip), never `logger.warning`/`error`.

    **Verification.** `app/strategy_engine/gate_conditions.py`'s own 10 pure unit tests (`test_gate_conditions.py`) — empty-dict-always-passes, `"regular"` satisfied/not-satisfied against real pre-market/regular/after-hours/weekend timestamps (real `MarketClock`, never mocked), and `validate_gate_conditions()` accepting the real condition and rejecting both an unrecognized key and an unrecognized value for a recognized key. `test_strategy_scheduler.py` gained 10 more: 4 registration-time (raises for an unrecognized key, error names the offending strategy, accepts the real condition, and the standing regression guard against the real 7-strategy registry), 5 per-candle pure (gated-and-blocked outside session, gated-and-fires inside session, an ungated strategy never blocked by this mechanism even outside session, gate check is per-strategy not a blanket skip, a gate-check exception doesn't block the strategy after it), and 1 DB-gated real-engine end-to-end test — real `EventBus`/`MarketStateEngine`/`ContextEngine`/`MarketClock`, a real pre-market `FeaturesUpdated` candle, asserting the gated stub's `evaluate()` is never called and nothing publishes, proving the exact failure mode this task set out to close, not a mocked stand-in for it. Full backend suite, real local Postgres (provisioned directly in this session — `apt-get install postgresql`, `alembic upgrade head` — same as decisions #113/#114): 598 total (578 baseline + 20 new), zero regressions. Two pre-existing, order/timing-sensitive failures flicker between runs exactly as already documented (decisions #114/#116) — `test_vwap_publishes_even_while_sma_is_still_warming_up` (consistent) and `test_daily_levels_carry_level_interaction_once_touched` (intermittent) — confirmed identical, both before this change and against a completely untouched second clone (`diff -rq` against that clone shows only the 2 files this entry touches plus the 2 new test files; nothing else changed).

    **Out of scope, confirmed and left alone:** `active_from`/`active_to` (D14/decision #116 — closed on separate terms, not reopened just because this handler now does more before `evaluate()`); Opportunity Engine ranking (§9); any strategy's own internal inline GATE step (four of seven now genuinely redundant with the new central check — noted above, not touched); any `gate_conditions` key beyond `"session"`.

---
