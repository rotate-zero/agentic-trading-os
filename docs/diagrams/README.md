# Diagrams

The diagram-focused [trading intelligence overview](trading-intelligence-overview.md) shows the live component links and compact views inside the major modules. The architecture and decisions in `../architecture/` remain the source of truth for detailed behavior.

This folder holds standalone rendered diagrams (Mermaid `.mmd`/`.md`, or exported `.svg`). Link them from the architecture section they illustrate and keep explanations with the canonical architecture document.
