"""Individual ContextProvider implementations — one module per provider,
registered into ContextEngine's provider list (engine.py). See
trading-intelligence-architecture.md §5 for the boundary rule each new
provider here needs to be checked against before being added.
"""
