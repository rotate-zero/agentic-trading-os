DEBUG=true

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=trading_workspace
POSTGRES_USER=trading
POSTGRES_PASSWORD=trading

MARKET_TIMEZONE=America/New_York

IBKR_HOST=127.0.0.1
IBKR_PORT=4002
IBKR_CLIENT_ID=1

# Optional — leave unset to skip Polygon auto-connect entirely.
# Free/Basic tier: 15-min delayed, 5 REST calls/min (see confirmed decision #30).
POLYGON_API_KEY=
POLYGON_POLL_INTERVAL_SECONDS=60
POLYGON_MAX_CALLS_PER_MINUTE=5

CORS_ALLOW_ORIGINS=["http://localhost:5173"]
